# Standar Kerja untuk Project Teleios (Konexa)

Project: WhatsApp Business Gateway + CRM SaaS (Laravel + backend Go untuk koneksi WhatsApp)

Dokumen ini adalah instruksi standing yang harus selalu diperhatikan di setiap sesi coding untuk project ini, bukan cuma sekali dibaca lalu dilupakan.

## Checklist Prioritas Kerja Saat Ini (diurutkan, per 21 Agustus 2026, update 27 Agustus 2026, update 2 September 2026)

Ini rangkuman semua task terbuka dari seluruh dokumen ini, diurutkan dari yang paling mendesak. Cek dari atas ke bawah tiap mulai sesi baru — jangan loncat ke bawah sebelum yang di atasnya beres, kecuali ada alasan jelas.

- [ ] **1. Commit & push semua perubahan hari ini ke git.** Composer.lock, fix migration (`disableForeignKeyConstraints`), fix seeder (hapus `WithoutModelEvents`), CLAUDE.md, dan `docs/api/wa-api-v1.openapi.json` semuanya masih cuma ada di disk lokal + VPS staging, BELUM ke-commit sama sekali. Ini paling berisiko — kalau ada apa-apa di laptop/VPS, semua kerjaan bisa hilang tanpa jejak. Command lengkap ada di section "Alur Branch & Deploy" langkah 2-4 di bawah.
- [ ] **2. `g_backend`: tambahkan panic recovery (`recover()`) di semua goroutine manual + event handler WhatsApp** — saat ini TIDAK ADA satupun `recover()` di codebase Go (lihat 9.1). Kalau 1 device di company manapun memicu panic, SELURUH proses mati dan WA SEMUA company ikut terputus bersamaan — dan karena proses production belum systemd (poin 13 di bawah), tidak auto-restart. Risiko tertinggi di seluruh audit karena dampaknya lintas-tenant, bukan cuma 1 company.
- [ ] **3. Sentralisasi proteksi pengiriman WA di `InboxService`** — menutup celah `WaApiSendMessageController` (lihat detail temuan di section "Task Menuju SaaS Siap Pakai" 7.1) sekaligus menghilangkan duplikasi cek di 3 file lain. **Scope bertambah per audit 27 Agustus**: `SendChatbotFlowMessages.php` dan `SendCsatSurvey.php` juga ternyata bypass total (lihat 9.5) — begitu sentralisasi ini selesai, keduanya otomatis ikut terlindungi tanpa kode tambahan, sama seperti `WaApiSendMessageController`. **Spesifikasi lengkap (siap eksekusi begitu ada instruksi "lanjut kerjakan"):**
  - Scope: method `send()`, `sendPoll()`, `sendMedia()`, `sendStoredMedia()` di `app/Services/Chat/InboxService.php` — keempatnya benar-benar mengirim ke Go backend.
  - Urutan pengecekan di dalam tiap method itu, SEBELUM panggil Go backend:
    1. `PackageLimitService::requireActivePackage($company)`
    2. `BroadcastThrottleService::attempt($deviceId, $companyId)`
    3. `PackageLimitService::reserve($company, $metric)` — default metric `broadcast_send`, buat parameter supaya bisa beda per caller (untuk kalau nanti auto-reply/AI bot dapat metric sendiri, lihat poin 8 checklist ini).
    4. Kalau kirim ke Go backend gagal → `release()` kuota yang tadi di-reserve; kalau sukses → biarkan.
  - Lempar exception spesifik per jenis kegagalan (package tidak aktif / kuota habis / throttle penuh) supaya tiap pemanggil bisa handle pesannya sesuai konteks (`WaApiSendMessageController` balas JSON 429/403, auto-reply/AI bot cukup skip diam-diam seperti sekarang).
  - Hapus pengecekan manual yang sekarang dobel di `SendScheduledWaMessage.php`, `SendAutoReplyMessage.php`, `SendAiBotReply.php` (baris `requireActivePackage()`/`reserve()`/`release()`/`throttle->attempt()`) — supaya tidak reserve kuota 2x per pesan. `WaApiSendMessageController`, `SendChatbotFlowMessages`, dan `SendCsatSurvey` otomatis ikut terlindungi begitu manggil `InboxService::send()`/`sendPoll()` dengan company+deviceId yang benar, tanpa tambahan kode di pemanggil itu sendiri.
  - WAJIB test di staging sebelum merge ke main: broadcast terjadwal, auto-reply, AI bot, chatbot flow, CSAT survey, dan WA API pihak ketiga — masing-masing 2 skenario (dalam kuota vs kuota habis, package aktif vs expired) — supaya jalur yang sudah jalan sekarang tidak ikut rusak.
  - **Belum boleh dikerjakan sebelum ada instruksi eksplisit** — masih tahap diskusi per aturan section "Alur Kerja Kolaborasi".
- [ ] **4. Tambah idempotency guard di `GoogleFormWebhookController`** — TIDAK otomatis ketutup oleh poin 3 di atas. Submit form yang terkirim dobel (retry Apps Script, dsb) saat ini bisa bikin pesan WA dobel ke customer. Ikuti pola `Cache::add($lockKey, true, ...)` yang sudah dipakai `WaIncomingMessageWebhookController` (lihat 9.5).
- [ ] **5. Hash `secret_key`/`token` di `WaApiKey`** — saat ini tersimpan plaintext di DB (lihat 9.3). Kebocoran DB/backup langsung expose semua kredensial API pihak ketiga semua company.
- [ ] **6. Tambah index `created_at`** pada `audit_logs`, `ledger_entries`, `history_user_login`, `voucher_histories`, `payment_transactions` — kelima tabel ini tumbuh terus lintas semua company dan selalu di-query `->latest()->paginate()` tanpa index (lihat 9.6). Migration tambah index, risiko rendah untuk dikerjakan.
- [ ] **7. Cek Superadmin > Package Limit** — pastikan semua paket yang dijual sudah punya angka limit untuk `device_count`, `contact_count`, `branch_count`, `broadcast_send`. Operasional, bukan coding. Selama belum diisi, semua company punya kuota unlimited tanpa disadari (lihat 7.2).
- [ ] **8. Putuskan kebijakan kuota auto-reply & AI bot reply** — sengaja unlimited (beda kebijakan dari broadcast terjadwal), atau perlu metric baru (`auto_reply_send`/`ai_bot_send`)? Ini nentuin apakah poin 3 di atas perlu metric tambahan (lihat 7.1).
- [ ] **9. Konsolidasi menu pengiriman pesan** (Visi Produk poin 1) — broadcast terjadwal + chatbot flow + jenis sejenis lain jadi satu form, tinggal pilih jenisnya di dalam. Refactor UX besar, cek dulu kondisi menu/controller aktual sebelum mulai, jangan asumsi.
- [ ] **10. Konsolidasi menu balas pesan** (Visi Produk poin 2) — AI, chatbot flow, auto-reply, balasan cepat jadi satu menu.
- [ ] **11. Fix `Superadmin\UserController::reset()`** — ganti update `wallet.balance` langsung jadi lewat `WalletLedgerService::debit()`, supaya konsisten tercatat di `LedgerEntry` seperti perubahan saldo lain (lihat 7.4). Risiko rendah, prioritas rendah.
- [ ] **12. Refactor `menu.blade.php`** (738 baris/48KB) jadi data-driven (array/config + 1 partial loop) supaya nambah/hapus menu tidak edit HTML manual panjang (lihat 7.3).
- [ ] **13. Systemd service + graceful shutdown untuk `g_backend_konexa` production** — proses jalan unmanaged (bare process sejak 19 Agustus), kalau mati tidak auto-restart (lihat Known Issues). **Digabung dengan 9.2**: tambahkan juga signal handling (SIGTERM/SIGINT) supaya koneksi WA semua device ditutup rapi saat restart/deploy, bukan di-kill paksa.
- [ ] **14. Tentukan use case & scope function-calling chatbot** (query/update data CRM lewat AI bot) — fitur baru besar, butuh keputusan use-case dulu sebelum desain teknis (lihat 7.5).
- [x] **15. Modul Jadwal Lanjutan ("Jadwal v2")** — pengembangan besar di atas modul Jadwal yang sudah ada (dibangun ulang 31 Agustus 2026, lihat riwayat migration `2026_08_31_140000` dst, modul lama sebelumnya sudah di-drop permanen 21 Agustus). Didiskusikan panjang dengan user 2 September 2026, user memberi instruksi eksplisit "lakukan pengembangan dan mulai coding" pada sesi 2 September 2026 — **SUDAH DIIMPLEMENTASIKAN PENUH (13 poin spesifikasi di bawah, semua selesai)**, ~60 file baru/diubah, semua lolos `php -l` + Blade balance check, **BELUM di-commit ke git** (lihat catatan status di akhir poin ini). Istilah penting supaya tidak rancu dengan kode yang sudah ada: yang user sebut **"Kelas"** (Piano, Drum, dst) = tabel `jadwal_mata_pelajaran` yang sudah ada; **"Kategori"** (Piano Classic Level 1/2, Piano Pop) = level BARU di bawah Kelas — sudah ada tabelnya sekarang (`jadwal_kategori`); **"Sesi"** = satu pertemuan bertanggal, yang di kode sekarang disebut `jadwal_kelas` (jangan disamakan dengan "Kelas" versi user).

  **Kenapa ini genuinely pengembangan baru, bukan "aktifkan fitur yang sudah ada":** `jadwal_kelas` hari ini cuma punya `start_time`/`end_time` (dateTime spesifik, bukan hari+jam berulang) — satu baris = satu sesi 1-on-1 yang dibuat manual satu-satu, tidak ada konsep jadwal rutin mingguan, tidak ada kolom harga/persentase/durasi di mana pun di aplikasi, `attendance_status` yang ada sekarang cuma 2 opsi (`hadir`/`tidak_hadir`) tanpa logika hangus/cap-bulanan, dan reminder WA (`JadwalReminderSetting.remind_target`) cuma bisa ke `parent`/`student`/`both` — **belum ada opsi ke pengajar sama sekali**. Notifikasi ke pengajar saat ini CUMA jalan lewat alur approve/reject Reschedule Request (`reschedule_notify_pengajar`) — kalau admin edit jadwal langsung dari form Edit biasa, pengajar TIDAK ke-notif (gap yang perlu ditutup juga).

  **Spesifikasi hasil diskusi (semua harus via setting, tidak ada yang hardcode):**

  1. **Jam Operasional per Branch** (setting baru) — hari buka (mis. Senin–Sabtu), jam buka–tutup, jam istirahat. Beda-beda per branch.
  2. **Ruangan** (entitas baru, per branch) — nama + catatan kegunaan (opsional, cuma info/saran — TIDAK mengunci ruangan ke kelas tertentu, karena satu ruangan bisa dipakai piano atau gitar akustik gantian).
  3. **Kategori** (entitas baru, di bawah Kelas/`jadwal_mata_pelajaran`) — tiap Kategori punya **harga per sesi sendiri** + **persentase split company/pengajar sendiri** (level fee di sini, BUKAN global — konfirmasi user: "Piano Classic Level 1, harga 400rb, Level 2 500rb, Piano Pop flat karena tidak ada level").
  4. **Jadwal Rutin murid** (entitas baru) — gantinya `jadwal_student` sekarang yang cuma bisa 1 mata-pelajaran+1 pengajar per baris, murid bisa punya BANYAK baris Jadwal Rutin: Kategori + Pengajar + Ruangan (opsional) + Hari + Jam mulai + Durasi. Ini "cetakan" mingguannya.
  5. **Validasi bentrok** (guru & ruangan) dicek **saat Jadwal Rutin dibuat/disimpan** (bukan nanti pas generate) — user: "harusnya tidak ada yang bentrok karena kan 1 kelas 1 ruangan 1 guru jadi sifatnya private", jadi validasi di titik input sudah cukup, tidak perlu resolver bentrok yang rumit saat generate.
  6. **Auto-generate Sesi otomatis** (bukan tombol manual) — sistem generate baris sesi bertanggal dari Jadwal Rutin tiap bulan, sesuai jam operasional branch, default **4 sesi/bulan**, minggu ke-5 (kalau hari itu jatuh 5x dalam sebulan) otomatis TIDAK dihitung/tidak digenerate sebagai sesi reguler — slotnya disisakan buat sesi pengganti (lihat poin 8). Angka "4 sesi/bulan" dan durasi default (mis. 30 menit) HARUS dari setting, bukan angka mati di kode.
  7. **Status kehadiran diperluas jadi 3 opsi** (dari 2 sekarang): **Hadir** / **Tidak Hadir — hangus** (tanpa keterangan, tidak ada pengganti, **pengajar tetap dibayar penuh** karena dia tetap hadir mengajar sesuai jadwal) / **Izin/Sakit — dapat pengganti**.
  8. **Sesi pengganti** (untuk kasus izin/sakit) — **dipilih manual oleh admin** (bukan sistem cari otomatis), dengan 2 pola umum yang biasa ditawarkan: (a) hari sama tapi digabung jadi durasi 1 jam, atau (b) diisi ke slot minggu ke-5 yang biasanya kosong (lihat poin 6). Rencana teknis: sesi pengganti jadi baris BARU yang terhubung/referensi ke sesi asli (bukan mengubah sesi asli), supaya histori "pernah izin tanggal X, penggantinya tanggal Y" tetap tercatat rapi untuk laporan — **ini masih asumsi saya, perlu dikonfirmasi ulang ke user sebelum desain final** kalau mulai dikerjakan.
  9. **Reminder WA ke pengajar** — H-1 OTOMATIS (rekap semua sesi besok: jam berapa sampai jam berapa, murid siapa saja) DAN bisa direquest manual kapan saja oleh pengajar lewat chat WA (mis. ketik "jadwal", sistem cocokkan nomor WA pengirim ke `users.handphone` si pengajar, balas rekap jadwal minggu ini). User eksplisit: **pengajar TIDAK perlu halaman dashboard baru untuk ini** ("pengajar sementara tidak perlu dashboard dulu, tapi bisa request by wa") — pakai infrastruktur auto-reply/device WA yang sudah ada, jangan bikin sistem baru.
  10. **Reminder manual oleh admin** — selain otomatis H-1, admin bisa klik kirim reminder kapan saja (per sesi/per murid/per pengajar) — device pengirim & template pesan tetap ambil dari SATU setting yang sama dengan reminder otomatis (jangan hardcode teks/nomor terpisah untuk jalur manual ini).
  11. **Notifikasi pengajar saat reschedule** — perluas supaya notif juga jalan kalau admin edit jadwal LANGSUNG lewat form Edit biasa (bukan cuma lewat approve/reject Reschedule Request seperti sekarang — lihat gap di paragraf pembuka poin ini).
  12. **Laporan Harian** (baru, bisa export Excel) — rekap hari ini: kelas/kategori apa saja yang terpakai, ruangan mana, daftar murid & pengajar, jam berapa saja.
  13. **Laporan Bulanan** (baru, bisa export Excel) — jumlah murid aktif/baru masuk, jumlah pergantian jadwal (reschedule) yang terjadi, total fee (breakdown persentase company vs tiap pengajar), lama mengajar (total jam) per pengajar.

  **Update setelah implementasi awal**: poin 12 & 13 di atas awalnya 2 menu terpisah (Laporan Harian & Laporan Bulanan), lalu atas permintaan user digabung jadi **1 menu "Laporan"** dengan filter rentang tanggal bebas (dari-sampai — satu hari kalau dari==sampai, satu bulan kalau dari=awal bulan sampai akhir bulan, atau rentang lain), export Excel 1 file 2 sheet (Detail Sesi + Rekap). Tombol Export & field tanggal ada di form yang sama (`formaction`), field tanggal wajib diisi (`required` + validasi server `JadwalLaporanController::export()`) — kalau export diklik tanpa tanggal terisi, redirect balik dengan pesan "Silakan pilih tanggal terlebih dahulu.". Lihat `JadwalLaporanService::sesiUntukRentang()`/`rekap()`, `JadwalLaporanExport`, `resources/views/jadwal/laporan/index.blade.php`.

  **Status implementasi (update per sesi coding setelah instruksi "lanjut kerjakan")**: Semua 13 poin di atas sudah dikerjakan — migration & model (Jam Operasional/`JadwalBranchSetting`, Ruangan/`JadwalRuangan`, Kategori/`JadwalKategori`, Jadwal Rutin/`JadwalRutin`, kolom snapshot baru di `jadwal_kelas`, `jadwal_pengajar_reminder_logs`), CRUD + validasi bentrok (`JadwalRutinConflictService`), command auto-generate sesi bulanan (`jadwal:generate-sesi`, skip minggu ke-5, scheduled tiap tanggal 1), status kehadiran 3 opsi + sesi pengganti (referensi via `pengganti_dari_sesi_id`), reminder WA ke pengajar (H-1 otomatis via `jadwal:dispatch-pengajar-reminders`, manual per-sesi oleh admin, dan request on-demand lewat keyword WA `pengajar_request_keyword` — semua reuse device/template dari `JadwalReminderSetting` yang sama, tidak ada yang hardcode), notifikasi pengajar saat admin edit jadwal langsung (bukan cuma lewat reschedule request), dan Laporan (1 menu, filter rentang tanggal dari-sampai) dengan export Excel 2 sheet.

  **Belum selesai / langkah berikutnya**: belum di-commit ke git sama sekali (masih di disk lokal user, lihat poin 1 checklist di atas soal risiko ini) — dan workflow branch project ini mewajibkan lewat `staging` dulu, bukan langsung ke `main` (lihat section "Alur Branch & Deploy"), jadi commit Jadwal v2 ini juga harus ikut alur itu. QA yang sudah dilakukan sejauh ini murni lint-level (`php -l` semua file PHP + balance-check semua Blade) dan cross-check route/view — belum ada review manual di browser (klik-klik UI, isi form beneran) ataupun test otomatis, jadi tetap disarankan dicoba dulu di staging sebelum dianggap 100% siap produksi.

  **Restrukturisasi drill-down 14 September 2026** (atas permintaan user, dikirim sebagai diagram): urutan navigasi index-ke-index diubah total jadi **Branch -> Ruangan -> Jam Operasional -> Mata Pelajaran / Bidang -> Kategori -> Pengajar -> Student**, tiap index cuma nampilin SATU tombol "+ Add <anak berikutnya>" + Edit/Delete (bukan berbagai tombol campur seperti sebelumnya). Poin penting biar tidak salah paham baca kode:
  - **Data Jam Operasional TIDAK pindah jadi per-Ruangan** — tetap App\Models\JadwalBranchSetting satu baris per `branch_office_id` seperti spec asli poin 1 di atas (`GenerateJadwalRutinSesi`/`JadwalRutinConflictService`/`JadwalKategori::hargaPerSesi()` semuanya masih baca `branchOffice->jadwalBranchSetting` sebagai satu baris, TIDAK disentuh). Yang pindah cuma TITIK MASUK-nya — tombol "Add Jam Operasional" sekarang di baris Ruangan (`JadwalRuanganController`/index.blade.php), lewat `JadwalBranchSettingController::index()` (baru) yang nampilin 0/1 baris branch itu. Mengedit dari Ruangan A akan mengubah baris yang SAMA dengan Ruangan B kalau satu branch — sudah ada catatan eksplisit di UI-nya (index.blade.php & edit.blade.php).
  - **Mata Pelajaran / Bidang TIDAK dapat FK baru** — tetap `branch_office_id` langsung seperti sebelumnya (spec asli, tidak berubah). `ruangan_id` cuma dibawa-bawa lewat query string (index/create/store) supaya tombol "Kembali" & "+ Tambah" bisa balik ke halaman Jam Operasional-nya Ruangan yang tadi dipakai masuk — murni navigasi, bukan kolom baru.
  - **Pengajar jadi entitas baru** — App\Models\JadwalPengajarKategori (tabel `jadwal_pengajar_kategori`, migration `2026_09_14_090300`), penugasan Pengajar (tetap `users` yang sama, lewat `ResolvesCompanyContext::companyTeamMembers()`) ke SATU Kategori (`unique(jadwal_kategori_id, pengajar_id)`). `JadwalPengajarController` yang sebelumnya cuma `index()` read-only (pilih dari daftar user, scoped ke Mata Pelajaran) sekarang full CRUD, scoped ke `jadwal_kategori_id`. Ketersediaan (hari & jam) ada di relasi terpisah — lihat poin "Ketersediaan Pengajar jadi banyak slot" di bawah, bukan lagi kolom langsung di tabel ini. Ketersediaan ini MURNI INFO — ditampilkan sebagai alert box di form Add Student (`JadwalStudentController::create()` lookup `JadwalPengajarKategori` by kategori+pengajar), TIDAK divalidasi silang ke `JadwalRutin` (validasi bentrok jadwal tetap 100% di `JadwalRutinConflictService`, tidak disentuh). Hapus baris Pengajar ini AMAN, tidak menghapus Jadwal Rutin/sesi yang sudah dibuat pengajar itu (referensi ke `users.id` langsung, tidak saling FK).
  - **Student tidak dapat kolom baru** — `jadwal_student` tetap persis sama (`jadwal_mata_pelajaran_id` + `pengajar_id`, tanpa `jadwal_kategori_id`). `jadwal_kategori_id` cuma query-string context (create/index/store) buat breadcrumb "Kembali ke Pengajar" & panel ketersediaan di atas, TIDAK disimpan ke tabel.
  - Tombol "Jadwal Rutin" di index Student (yang berfungsi sebagai "lihat jadwal per student" di diagram) **sudah ada dari sebelumnya** (`route('jadwal.rutin.index', ['student_id' => ...])`), tidak perlu dibuat baru — begitu juga index `jadwal-kelas` dengan tab per Ruangan (bagian bawah diagram) yang sudah dikerjakan di sesi sebelumnya, tidak disentuh restrukturisasi ini.
  - QA restrukturisasi ini juga murni lint-level (`php -l` semua file yang diubah + balance-check semua Blade yang diubah) — belum diklik manual di browser, ikut catatan "belum selesai" di atas.
  - **Update 3 September 2026 (masih permintaan user, sesi yang sama): Pengajar dapat menu sidebar sendiri.** Sebelumnya Pengajar cuma bisa dijangkau lewat drill-down "+ Add Pengajar" di index Kategori (`jadwal_kategori_id` selalu ada). Sekarang ada juga entry "Pengajar" langsung di sidebar (`resources/views/layouts/partials/menu.blade.php`, antara "Mata Pelajaran / Bidang" dan "Student") yang mengarah ke `route('jadwal.pengajar.index')` TANPA query string — mode "global", pola sama seperti `JadwalMataPelajaranController`/`JadwalStudentController` yang sudah ada (bare = semua baris company/branch + kolom Kategori tambahan, scoped via `jadwal_kategori_id` = filter ke satu Kategori seperti sebelumnya). `JadwalPengajarController::index()/create()/store()/edit()/update()` semuanya sudah menangani `$kategori` null; `edit()` SELALU dropdown Kategori bebas (tidak pernah locked), `create()` locked kalau datang dengan `jadwal_kategori_id` valid, bebas kalau tidak — pola locked-vs-free "ina" project's University Album Photo yang sama dipakai di form lain. Blade view `jadwal-pengajar/{index,create,edit,_form}.blade.php` diupdate seluruhnya supaya tidak null-property-access saat `$kategori` null. "Notifikasi jika tidak bisa add" (poin eksplisit dari user) diimplementasi di `create.blade.php`: kalau belum ada Kategori sama sekali (mode global, dropdown bakal kosong) atau belum ada Team Member yang eligible jadi pengajar, form disembunyikan diganti alert warning yang jelas + link ke halaman prasyaratnya, bukan nampilin dropdown kosong yang bakal gagal validasi diam-diam.
  - **Update 3 September 2026 (masih sesi yang sama): Ketersediaan Pengajar jadi BANYAK slot (hari + jam), bukan satu hari_bisa[] + satu jam.** Kasus lapangan dari user: pengajar TIDAK available nonstop pagi-sore seperti jam kantor — satu hari bisa punya lebih dari satu rentang jam (mis. Senin 10:00-12:00 lalu 17:00-19:00 di hari yang sama), dan tiap hari bisa beda-beda. Struktur lama (kolom `hari_bisa` json + satu `jam_mulai`/`jam_selesai` langsung di `jadwal_pengajar_kategori`) tidak bisa menampung itu (satu jam berlaku sama ke semua hari yang dicentang). Kolom `hari_bisa`/`jam_mulai`/`jam_selesai` dihapus dari migration `2026_09_14_090300` (asumsi awal saat itu: migration ini belum pernah `php artisan migrate` — **asumsi ini TERNYATA SALAH**, lihat poin "Fix 3 September 2026: kolom lama hari_bisa masih nyangkut di production" di bawah untuk koreksinya). Ketersediaan sekarang di tabel anak baru `jadwal_pengajar_kategori_jadwal` (migration `2026_09_14_090400`, model App\Models\JadwalPengajarJadwal) — satu baris = satu `hari` (int, konvensi sama Carbon::dayOfWeek) + satu `jam_mulai`/`jam_selesai`, relasi `hasMany` dari `JadwalPengajarKategori::jadwals()`. Boleh banyak baris di hari yang sama, tidak ada unique constraint di situ (validasi cuma jam_selesai > jam_mulai per baris, lewat `$validator->after()` di `JadwalPengajarController::validator()`, bukan rule `after:` bawaan supaya tidak bergantung ke perilaku wildcard sibling-field Laravel). `JadwalPengajarKategori::jadwalGroupedByHari()`/`jadwalSummaryLabel()` — helper buat nampilin slot dikelompokkan per hari (dipakai di index Pengajar & panel ketersediaan form Add Student). Form (`jadwal-pengajar/_form.blade.php`) sekarang list baris dinamis "+ Tambah Baris" (vanilla JS, pola sama seperti preview harga di `jadwal-kategori/_form.blade.php`) — tiap baris: dropdown Hari + input jam mulai + input jam selesai, bukan checkbox hari + satu jam gabungan. `JadwalPengajarController::syncJadwal()` — hapus semua slot lama lalu buat ulang dari input form tiap kali store()/update() (dibungkus `DB::transaction`), lebih sederhana & aman daripada diff per-baris karena jumlah baris bebas berubah dari UI.
  - **Fix 3 September 2026: kolom lama `hari_bisa` masih nyangkut di production, bikin insert Pengajar gagal.** Setelah poin di atas dideploy, `store()` Pengajar gagal di server dengan `SQLSTATE[HY000]: 1364 Field 'hari_bisa' doesn't have a default value`. Penyebabnya: migration Laravel dicatat per NAMA FILE, bukan isinya — migration `2026_09_14_090300_create_jadwal_pengajar_kategori_table` **TERNYATA sudah pernah jalan di server user SEBELUM sesi ini** (kontradiksi asumsi yang ditulis di poin sebelumnya), jadi waktu isi file itu diedit untuk menghapus kolom `hari_bisa`/`jam_mulai`/`jam_selesai`, `php artisan migrate` MELEWATI migration itu (dianggap sudah pernah dijalankan) — kolom lama itu TETAP ada di tabel `jadwal_pengajar_kategori` yang sungguhan, sementara `App\Models\JadwalPengajarKategori::create()` sudah tidak mengisinya lagi. **Pelajaran: mengedit migration yang mungkin sudah pernah `migrate` di lingkungan MANA PUN (bukan cuma lokal) itu tidak bisa diandalkan** — kalau ragu apakah migration sudah pernah jalan di server production, JANGAN edit file migration yang sudah ada, buat migration ALTER baru. Fix-nya: migration baru `2026_09_14_090500_drop_legacy_jam_columns_from_jadwal_pengajar_kategori_table` yang benar-benar `DROP COLUMN` ketiga kolom itu dari tabel yang sudah ada, dibungkus `Schema::hasColumn()` per kolom supaya aman di DUA skenario — production yang masih punya kolom lama (dihapus) MAUPUN instalasi baru yang migration 090300-nya sudah versi final tanpa kolom itu sejak awal (no-op).
  - **Update 3 September 2026 (masih sesi yang sama): form "Add Student" bisa auto-generate Jadwal Rutin dari slot Pengajar yang dicentang.** Permintaan user: begitu di form Add Student (datang dari drill-down Kategori→Pengajar→+Add Student, jadi ada konteks Kategori+Pengajar), tampilkan checklist slot ketersediaan Pengajar (App\Models\JadwalPengajarJadwal) yang MASIH KOSONG saja, admin tinggal centang, dan begitu Student disimpan sistem langsung bikin Jadwal Rutin + generate sesi bulan ini juga (bukan nunggu cron bulanan) — "misal 10:00-12:00 = 4x", mengacu ke `sesi_per_bulan_default` yang sudah ada.
  - **Update 3 September 2026 (koreksi poin di atas, sesi yang sama): checklist dipecah per durasi sesi default branch, bukan satu checkbox per blok mentah.** Setelah lihat hasil poin di atas jalan (checkbox tunggal "Jumat, 13:30 - 17:00" untuk satu blok ketersediaan Pengajar yang dideklarasikan), user koreksi: satu blok panjang begitu HARUS dipecah jadi beberapa checkbox terpisah sepanjang `JadwalBranchSetting::durasi_sesi_default_menit` (mis. 30 menit) — "Senin jam 13.00 s/d 13.30", "Senin jam 13.30 s/d 14.00", dst — supaya murid BEDA-BEDA bisa isi jam berbeda-beda di dalam rentang ketersediaan yang sama, bukan satu murid mengambil seluruh blok. Implementasi di `App\Http\Controllers\Jadwal\JadwalStudentController`: `splitSlotIntoChunks(jamMulai, jamSelesai, durasiMenit)` — pure function pecah satu rentang jadi list chunk berdurasi tetap, sisa di ujung blok yang kurang dari satu durasi penuh SENGAJA dibuang (tidak jadi chunk parsial). `openSlotsFor()` sekarang `flatMap` tiap slot induk (App\Models\JadwalPengajarJadwal) ke chunk-chunknya dulu baru filter jam operasional + cek bentrok PER CHUNK (bukan per blok), dan tiap chunk dapat `id` checkbox berformat gabungan string `"{parent_slot_id}|{H:i jam mulai chunk}"` (bukan uuid chunk sungguhan — chunk tidak disimpan di database, murni hasil split on-the-fly tiap request). `parseChunkId()` mem-parse balik format itu (return null kalau format tidak valid = kemungkinan request di-tempering). `createRutinFromSlots()` (parameter `$chunkIds`, sebelumnya `$slotIds`) untuk tiap chunk yang dicentang: verifikasi ulang slot induknya memang milik company+kategori+pengajar yang benar, RE-DERIVE ulang daftar chunk yang sah dari rentang asli slot induk (bukan percaya begitu saja `jam_mulai` yang dikirim form) dan cuma proses kalau submisi cocok persis salah satu hasil split ulang itu, baru cek jam operasional branch + bentrok pengajar (`JadwalRutinConflictService::findPengajarConflict()`) seperti sebelumnya — `JadwalRutin::durasi_menit` diisi langsung dari `durasi_sesi_default_menit` (bukan diff jam_mulai/jam_selesai lagi, karena sekarang sudah pasti seukuran chunk). View `jadwal-student/create.blade.php` TIDAK perlu berubah — checkbox `value="{{ $slot['id'] }}"` & label `{{ $slot['jam_mulai'] }} - {{ $slot['jam_selesai'] }}` tetap kerja transparan dengan `id` berformat gabungan baru ini.
  - **Update 7 September 2026 (revisi final, sesi yang sama): "Jadwal Kelas" (`jadwal.kelas.index`) DIGANTI TOTAL dari grid slot 30 menit menjadi tabel flat berpaginasi dengan filter Tanggal/Pengajar/Bidang-Mata-Pelajaran, plus validasi baru jam operasional.** Riwayat 2 iterasi di sesi yang sama: (1) awalnya dicoba mode pengelompokan grid per Pengajar (di samping grid per Ruangan yang sudah ada) — sempat dibangun & di-commit; (2) user lalu minta diskusi ulang ("kita diskusi dulu ya") dan menjelaskan maksud sebenarnya: "bagaimana cara nya menampilkan semua guru, semua ruangan, dan student ... saya rasa tampilannya begitu saja, by filter: tanggal, pengajar, bidang/mata pelajaran ... tampilkan data relasinya pakai data table ya dan button action saja". Dikonfirmasi via pertanyaan eksplisit: **ganti total** (bukan halaman terpisah / bukan tambahan mode), dan **"jam guru input itu harus sama dengan jam operasional branch"** berarti validasi baru (bukan sekadar konteks) — sesi yang jam-nya diinput manual di luar Jam Operasional branch (`JadwalBranchSetting`) ditolak. Implementasi final: `App\Http\Controllers\Jadwal\JadwalKelasController::index()` sekarang query `JadwalKelas` langsung (BUKAN slot sintetis) — 1 baris = 1 sesi nyata, filter `date`/`pengajar_id`/`jadwal_mata_pelajaran_id`, `paginate(20)`. `buildSlotGrid()` dan partial `jadwal-kelas/_slot-grid-table.blade.php` DIHAPUS; markup tabelnya dipindah langsung ke `index.blade.php` (kolom: Pengajar, Ruangan, Bidang, Kategori, Murid, Mulai, Selesai, Kehadiran, Status, Aksi). `group_by`/`gridRedirectParams()`/`CompanyToUser::branch_office_id`-untuk-grid semuanya hilang; diganti `filterRedirectParams(JadwalKelas $kelas): array` (return `date`/`pengajar_id`/`jadwal_mata_pelajaran_id` dari sesi itu sendiri) yang dipakai create()/edit()/store()/update()/destroy() supaya admin balik ke filter yang relevan dengan sesi tsb setelah submit/hapus. **Validasi baru** (di `validator()`, lewat `$validator->after()` closure karena butuh `branch_office_id` + `start_time`/`end_time` sekaligus): kalau `start_time` diisi DAN branch itu punya `JadwalBranchSetting`, dicek `isHariOperasional()` (hari kerja branch) dan `isWithinOperationalHours()` (jam buka/tutup + jam istirahat) — sesi yang menabrak salah satu ditolak dengan pesan error spesifik. Validasi di-skip kalau `start_time` kosong atau branch tidak punya `JadwalBranchSetting` sama sekali (tidak memblokir branch yang belum setup jam operasional).
    - **"Kosong"** = tidak bentrok Jadwal Rutin aktif punya Pengajar yang sama (rule "1 kelas 1 guru = private" yang sudah ada di App\Services\Jadwal\JadwalRutinConflictService, dipakai App\Http\Controllers\Jadwal\JadwalRutinController) DAN dalam jam operasional branch (App\Models\JadwalBranchSetting). `JadwalRutinConflictService::findPengajarConflict()` (baru, wrapper publik ke `findConflict()` privat yang sudah ada) dipakai untuk cek ini di luar jalur `check()` yang penuh pesan error.
    - `JadwalStudentController::create()` menghitung `openSlots` (helper privat `openSlotsFor()`) dari `JadwalPengajarKategori::jadwals` yang sudah ada, filter jam operasional + reject yang bentrok. `store()` **VALIDASI ULANG SEMUANYA** dari nol lewat `createRutinFromSlots()` (bukan percaya `$openSlots` dari form) -- kepemilikan slot (company+kategori+pengajar cocok lewat `whereHas('pengajarKategori', ...)`), jam operasional, DAN bentrok ulang (race condition: slot kelihatan kosong saat form dibuka bisa saja baru terisi admin lain sebelum submit). Slot yang gagal (bukan error, cuma di-skip) dilaporkan di flash message "Slot yang dilewati: ...", Student-nya sendiri TETAP berhasil dibuat.
    - Tiap slot yang lolos jadi 1 baris App\Models\JadwalRutin: `hari`/`jam_mulai` dari slot, `durasi_menit` = selisih jam_mulai-jam_selesai slot (jadi SATU sesi = seluruh blok yang dideklarasikan Pengajar, bukan durasi default branch), `efektif_mulai` = hari ini, `jadwal_ruangan_id` = null (form ini belum ada pilihan Ruangan). Semua dibungkus SATU `DB::transaction()` bareng `JadwalStudent::create()` di `store()`.
    - **Generate sesi langsung (bukan nunggu tanggal 1)**: logic generate-per-baris di-extract dari `App\Console\Commands\GenerateJadwalRutinSesi` ke service baru `App\Services\Jadwal\JadwalRutinSesiGenerator` (`generateForRutin()` untuk panggilan langsung 1 baris + bulan berjalan, `generateForRutinAndBranchSetting()` untuk dipakai command yang looping banyak baris sekaligus lewat `chunkById()`) -- SATU sumber logic dipakai DUA jalur (command bulanan & langsung dari `JadwalStudentController::store()`), idempotent (unique index `jadwal_rutin_id`+`start_time` tetap yang sama), jadi tidak ada sesi dobel walau nanti command bulanan tetap jalan juga.
    - Kalau branch belum punya Jam Operasional sama sekali, form tetap menampilkan Student form seperti biasa (checklist-nya diganti alert warning + Student tetap bisa dibuat) -- fitur ini murni tambahan opsional, bukan syarat wajib nambah Student.
  - **Update 4 September 2026 (permintaan user, 4 perbaikan drill-down Jadwal sekaligus):**
    - **Branch dikunci otomatis di "Tambah Student" saat datang dari drill-down.** Urutan drill-down Jadwal itu Branch → Ruangan → Jam Operasional → Mata Pelajaran / Bidang → Kategori → Pengajar → Student — pada titik "Tambah Student", branch SEHARUSNYA sudah ditentukan lewat Mata Pelajaran / Bidang yang dipilih di atasnya, jadi field Branch di form itu tidak perlu jadi pilihan bebas lagi. `JadwalStudentController::create()` sekarang resolve `JadwalMataPelajaran` dari `jadwal_mata_pelajaran_id` di query string (kalau ada) lalu kunci `selectedBranchOfficeId` dari `branch_office_id` MILIK MATA PELAJARAN ITU SENDIRI (bukan dari query string `branch_office_id` — supaya tidak bisa di-tempering beda dari Mata Pelajaran yang sebenarnya dipilih). Mata Pelajaran yang branch_office_id-nya null (tidak terikat branch manapun) tidak mengunci apa-apa, tetap dropdown bebas seperti sebelumnya. `_form.blade.php` tidak berubah -- mekanisme locked-vs-free `$selectedBranchOfficeId` di situ sudah generic dari sebelumnya, cuma sebelumnya controller `create()` tidak pernah mengisinya sama sekali.
    - **Badge "Jumlah Ruangan" baru di index Mata Pelajaran / Bidang.** Sebelumnya badge Statistik cuma Kelas/Pengajar/Murid/Kategori -- badge "Kelas" (jumlah baris Jadwal Kelas) kepencet diartikan user sebagai jumlah Ruangan, padahal beda konsep (kelas = jumlah sesi, ruangan = jumlah ruangan unik yang dipakai). `JadwalMataPelajaranController::index()` tambah `addSelect(['ruangan_count' => ...COUNT(DISTINCT jadwal_ruangan_id)...])`, logic sama persis dengan `pengajar_count`/`student_count` yang sudah ada (COUNT DISTINCT otomatis skip NULL). Badge baru (ikon `ri-door-open-line`) disisipkan di antara badge Kelas dan Pengajar di `index.blade.php`.
    - **"Jumlah Murid" di index Pengajar, diklik pindah ke list Student.** `JadwalPengajarController` tambah kolom baru "Murid" (badge ikon `ri-graduation-cap-line`) di tabel `jadwal-pengajar/index.blade.php`, antara "Hari & Jam Tersedia" dan "Status" -- diklik navigasi ke `jadwal.student.index` sudah ter-filter (`jadwal_mata_pelajaran_id` + `pengajar_id` + `jadwal_kategori_id` untuk breadcrumb), pola link yang sama dengan tombol "Add Student" yang sudah ada di baris yang sama. Dihitung method baru `attachMuridCounts()` -- `App\Models\JadwalStudent` TIDAK menyimpan `jadwal_kategori_id` (cuma `jadwal_mata_pelajaran_id` + `pengajar_id`), jadi "murid dari Kategori ini" didekati dari pasangan (pengajar_id, Mata Pelajaran milik Kategori itu), SATU query dikelompokkan (bukan query per baris di dalam loop) supaya tidak N+1 walau paginate 15 baris.
    - **Tombol "Jadwal Rutin" & "Add Jadwal" DIHAPUS dari baris Student**, `jadwal-student/index.blade.php` Aksi disederhanakan jadi Edit + Delete saja (pola sama seperti baris Pengajar). Alasan user: "Add Jadwal" (ke `JadwalKelasController::create()`, sesi manual bebas) TIDAK dicek bentrok pengajar sama sekali (cuma validasi jam operasional, lihat item "Update 7 September 2026" di atas) -- jadi jalan pintas yang bisa bikin jadwal murid bentrok tanpa lewat pengecekan ketersediaan Pengajar yang semestinya. Admin yang mau kelola Jadwal Rutin/Jadwal Kelas langsung tetap bisa lewat menu sidebar-nya sendiri, cuma shortcut per-baris di Student yang hilang.
    - **Checklist slot ketersediaan Pengajar (bukan cuma info) baru di halaman Edit Student**, menggantikan fungsi kedua tombol yang dihapus di atas -- revisi KEDUA di sesi yang sama: draft pertama cuma panel read-only (teks ringkasan "Classic: Senin 13:00-17:00, ..."), lalu user minta lihat gambar checklist create() dan bilang "dibawah pengajar dikeluarkan jadwal si pengajar seperti gambar ke 2" + "kalau sudah ada yang isi tidak boleh di ceklist lagi atau di buat disable tapi tetap tampilkan" -- jadi checklist beneran (bisa dicentang, bikin Jadwal Rutin baru), plus permintaan slot yang sudah kepakai TETAP tampil tapi disabled (bukan hilang dari daftar), berlaku di KEDUA halaman (Tambah Student & Edit Student), dan Pengajar cuma SATU dropdown (bukan 2 terpisah seperti draft pertama).
      - `slotsFor()` (rename dari `openSlotsFor()`) sekarang TIDAK `reject()` chunk yang bentrok lagi -- semua chunk dalam jam operasional dikembalikan, masing-masing dapat `taken` (bool) + `taken_by` (nama murid yang sudah pakai, null kalau tidak taken). Terima parameter baru `$excludeStudentId` supaya murid yang SEDANG di-edit tidak dianggap "bentrok dengan dirinya sendiri" untuk slot yang memang sudah jadi Jadwal Rutin-nya dia (null di `create()`, murid belum ada). `createRutinFromSlots()` (dipanggil saat submit, di `store()` MAUPUN `update()`) ikut dapat pengecualian yang sama di pengecekan bentroknya.
      - Partial baru `_slot-checklist.blade.php` (dipakai `create.blade.php` DAN `edit.blade.php`, satu sumber markup) -- checkbox `taken=true` di-`@disabled` + label dicoret + badge "sudah dipakai — {nama murid}", TIDAK dihapus dari daftar. Checkbox disabled browser tidak pernah ikut ter-submit, jadi tidak ada celah keamanan dari perubahan ini -- `createRutinFromSlots()` tetap re-validasi ulang dari nol seperti sebelumnya.
      - Edit Student: dropdown Pengajar di `_form.blade.php` (shared partial create+edit) SENDIRI yang jadi trigger reload checklist -- `onchange` JS navigasi GET ke `jadwal.student.edit` yang sama dengan `?pengajar_id=...` baru, HANYA aktif kalau `$previewPengajarId` ada (cuma di-set `edit()`, jadi tidak mempengaruhi `create()` sama sekali). Method baru `pengajarSlotsPanel()` (ganti `pengajarAvailabilityPanel()`) mengumpulkan SEMUA `JadwalPengajarKategori` Pengajar itu (karena `JadwalStudent` tidak menyimpan Kategori), tiap baris dapat properti `slots` hasil `slotsFor()`. Jam Operasional dipakai dari branch Student ITU SENDIRI (`$student->branch_office_id`, satu sumber konsisten lintas Kategori). Checkbox dikirim terkelompok per Kategori: `jadwal_rutin_slot_ids[<jadwal_kategori_id>][]` -- `update()` loop tiap grup lewat `createRutinFromSlots()` yang SAMA dipakai `store()`, dibungkus satu `DB::transaction()` bareng update profil Student.
      - **Bug fix 4 September 2026 (masih sesi yang sama, laporan user setelah tes): "jadwal yg dipilih saat pertamaa kali create tidak keluar ketika ingin melakukan edit".** Penyebab: `slotsFor()` cuma punya flag `taken`/`taken_by`, jadi slot yang sudah jadi Jadwal Rutin AKTIF milik murid yang sama (dikecualikan dari "bentrok" lewat `$excludeStudentId`) berakhir `taken = false` -- terlihat sama seperti slot kosong biasa, checkbox-nya TIDAK pernah tercentang otomatis di Edit Student (checklist cuma baca `old()`, yang kosong di GET pertama). Fix: `slotsFor()` sekarang juga mengembalikan `mine` (bool) -- true kalau slot itu bentrok dengan Jadwal Rutin aktif TAPI punya murid yang sama persis dengan `$excludeStudentId`. `_slot-checklist.blade.php` menandai slot `mine => true` TERCENTANG + `@disabled` (sama seperti `taken`) + badge hijau "jadwal aktif murid ini" -- disabled supaya checkbox itu tidak ikut ter-submit sama sekali (mencegah `createRutinFromSlots()` bikin baris `JadwalRutin` DUPLIKAT untuk slot yang sudah ada kalau tercentang lalu form disimpan ulang). `createRutinFromSlots()` sendiri dapat guard tambahan (jaga-jaga request di-tempering): kalau chunk yang dicentang ternyata bentrok dengan Jadwal Rutin aktif milik MURID YANG SAMA, dilewati diam-diam (bukan dianggap error/skip yang dilaporkan), bukan dibuat baris baru.
      - **Revisi lagi 4 September 2026 (masih sesi yang sama, permintaan user): murid harus bisa GANTI jadwal dari Edit Student, bukan cuma nambah.** User eksplisit: "ceklist hanya penanda saja... jadwal yang lain [yang] sudah terisi itu di disable [saja]" dan setelah ditanya soal cara "menghapus" jadwal lama, jawab tegas "jangan di nonaktifkan[,] murid itu hanya pengen ganti jadwal nya tapi masih tetap jadi murid" (jadi bukan soft-deactivate) serta "biarkan apa adanya" untuk sesi (JadwalKelas) bulan ini yang sudah ter-generate dari jadwal lama. Implementasi: slot `mine` di `_slot-checklist.blade.php` TIDAK lagi `@disabled` (beda dari draft sebelumnya) -- checkbox-nya BISA di-uncheck, checked-state defaultnya dibedakan lewat `$errors->any()` (redisplay setelah gagal validasi vs buka pertama kali/reload ganti Pengajar). `JadwalStudentController::update()` sekarang me-reconcile: `pengajarSlotsPanel()` dihitung ULANG dari server (bukan percaya submitted array) untuk tahu slot `mine` mana yang tadinya aktif tapi tidak ikut ter-submit lagi -- baris `JadwalRutin`-nya **dihapus permanen** (bukan dinonaktifkan, sesuai keputusan user), status Student itu sendiri TIDAK tersentuh sama sekali. Sesi (`JadwalKelas`) bulan ini yang sudah ter-generate dari baris itu SENGAJA tidak ikut dihapus (FK `jadwal_rutin_id` sudah `nullOnDelete()`, konsisten dengan `JadwalRutinController::destroy()` yang juga begitu).
      - **Bug fix lanjutan 4 September 2026 (masih sesi yang sama, laporan user: "pada form tambah student tidak keluar ya jadwal pengajar nya").** Penyebab: panel ketersediaan Pengajar di "Tambah Student" SEBELUMNYA cuma muncul kalau `jadwal_kategori_id` DAN `pengajar_id` datang SEKALIGUS lewat query string (drill-down PENUH Kategori→Pengajar→"+ Add Student" dari index Pengajar) -- akses lain (menu sidebar "Student" langsung, atau habis klik "Ganti Pengajar"/"Ganti Mata Pelajaran / Bidang") sama sekali tidak menampilkan apa-apa walau admin sudah pilih Pengajar di dropdown bebas, beda dari Edit Student yang dropdown Pengajar-nya sendiri sudah bisa reload checklist. Root cause KEDUA yang ikut ketemu: dropdown Pengajar di `_form.blade.php` ternyata SELALU terkunci (disabled) begitu `$selectedPengajarId`/`old('pengajar_id')` ada nilainya -- termasuk di skenario dropdown bebas yang baru reload lewat query string, jadi admin tidak pernah bisa ganti-ganti Pengajar buat lihat checklist lain. Fix: `JadwalStudentController::create()` sekarang pakai SATU query yang menangani DUA skenario -- `jadwal_kategori_id` ada (drill-down penuh) → difilter ke SATU Kategori itu saja (perilaku lama, tidak berubah); tidak ada → tampilkan SEMUA Kategori yang Pengajar itu ajar (pola sama dengan `pengajarSlotsPanel()` Edit Student), Jam Operasional di-resolve PER BARIS (beda dari Edit yang punya satu sumber `$student->branch_office_id`, karena create() belum ada Student yang committed). `_form.blade.php` dapat parameter baru `$pengajarLocked` (bool, eksplisit dari controller -- create() true HANYA di drill-down penuh, edit() SELALU false) yang menggantikan pengecekan lama "ada nilai = terkunci"; dropdown Pengajar bebas sekarang SELALU dapat `onchange` reload (dulu cuma Edit yang punya, lewat pengecekan `isset($previewPengajarId) && isset($student)`) -- target reload beda per form (`jadwal.student.edit` utk Edit, `jadwal.student.create` sambil bawa `jadwal_mata_pelajaran_id`/`jadwal_kategori_id` yang masih terkunci utk Create), dengan pemilihan separator `?`/`&` otomatis supaya tidak nubruk query string yang sudah ada. `store()` ikut disesuaikan memproses `jadwal_rutin_slot_ids[<kategoriId>][]` terkelompok (format yang sama dipakai `update()`), bukan lagi array datar + satu `jadwal_kategori_id` hidden field tunggal. `create.blade.php` checklist-nya ikut jadi grouped-per-Kategori (persis `edit.blade.php`), alert box ringkasan ketersediaan yang lama (redundan dengan checklist) dihapus.
      - **Update tampilan 4 September 2026 (masih sesi yang sama, permintaan user): "tampilkan category nya di form dan di index nya student", "jam pengajar diganti dengan tab", "form nya mungkin bisa di perbesar".** Tiga perubahan murni tampilan (tidak ada perubahan skema/data):
        - **Kolom "Kategori" baru di index Student.** `JadwalStudent` TIDAK menyimpan `jadwal_kategori_id` (lihat class docblock), jadi kolom ini DI-DERIVE dari `App\Models\JadwalRutin` AKTIF milik Student itu (bisa lebih dari satu badge kalau Student punya beberapa Jadwal Rutin di Kategori berbeda, `-` kalau belum punya Jadwal Rutin aktif sama sekali). `JadwalStudentController::index()` menghitung SATU query dikelompokkan per `student_id` (bukan query di dalam loop, supaya tidak N+1 walau paginate 15 baris) lalu attach sebagai `$student->kategori_names` (Collection nama Kategori unik).
        - **Checklist ketersediaan Pengajar (Tambah & Edit Student) diganti jadi Bootstrap nav-tabs, satu tab per Kategori** -- sebelumnya ditumpuk vertikal (bisa panjang kalau Pengajar ngajar banyak Kategori). Partial baru `_kategori-tabs.blade.php` (SATU sumber dipakai `create.blade.php` DAN `edit.blade.php`, delegasi ke `_slot-checklist.blade.php` yang sudah ada per tab-pane) -- label tab itu sendiri yang jadi cara "tampilkan Kategori di form" (satu fitur, bukan field terpisah). Kalau cuma SATU Kategori, tab di-skip (langsung checklist, tanpa chrome tab yang tidak perlu). Tab yang punya slot `mine` (Edit Student) dapat badge hijau berisi jumlahnya. Tab yang aktif default: kalau redisplay setelah gagal validasi (`$errors->any()`) DAN ada Kategori yang slot-nya barusan disubmit lewat `old()`, tab itu yang dibuka duluan (bukan selalu tab pertama) -- supaya centangan yang baru diubah admin tidak "hilang dari pandangan". `pengajarSlotsPanel()` (dipakai Edit) ikut di-update, tiap `$pk` sekarang dapat atribut `branchSettingMissing` PER BARIS juga (sebelumnya cuma flag global) supaya bentuknya konsisten dengan `create()` untuk dikonsumsi `_kategori-tabs.blade.php`.
        - **Card form Tambah & Edit Student diperlebar** dari `col-lg-6` jadi `col-lg-8` (create.blade.php & edit.blade.php) -- lebih pas menampung tab + checklist yang sekarang bisa beberapa kolom.
      - **Revisi lagi 4 September 2026 (masih sesi yang sama, dari screenshot Edit Student user -- daftar Senin+Jumat ditumpuk jadi satu list panjang): "jam pengajar apakah bisa di buat tab aja", "tulisan optional nya di hapus saja".** Slot ketersediaan di `_slot-checklist.blade.php` sekarang dikelompokkan per HARI juga (bukan cuma per Kategori lewat `_kategori-tabs.blade.php`) -- kalau Pengajar tersedia di LEBIH DARI SATU hari, ditampilkan sebagai tab (satu tab per hari, sama pola dengan tab Kategori); kalau cuma satu hari, tetap datar seperti sebelumnya (tab untuk satu pilihan cuma nambah klik). Baris checkbox di-extract ke partial baru `_slot-checkbox.blade.php` (dipakai ulang di mode datar maupun mode tab) -- labelnya TIDAK lagi mengulang nama hari di tiap baris (cuma jam mulai-selesai), karena nama hari sekarang cuma ditulis SEKALI (judul kalau datar, label tab kalau bertab). Tab hari yang aktif default ikut logic yang sama seperti tab Kategori (redisplay gagal validasi → hari yang slot-nya barusan disubmit; kalau tidak → hari yang punya slot `mine`; fallback hari pertama), dan tab yang punya slot `mine` dapat badge hijau jumlahnya. Partial ini SATU sumber dipakai Tambah & Edit Student (di dalam tab Kategori kalau ada, atau langsung kalau cuma satu Kategori), jadi efeknya otomatis kena dua-duanya. Label "Jadwal Rutin Murid Ini" di `edit.blade.php` dihapus kata "(opsional)"-nya (permintaan eksplisit, khusus Edit Student -- label Tambah Student tidak diubah).
  - **Update 4 September 2026 (masih sesi yang sama, permintaan baru user): "Jadwal Kelas" (`jadwal.kelas.index`) diganti LAGI dari tabel flat balik jadi GRID per jam, di-tab per Ruangan -- iterasi KEEMPAT di riwayat halaman ini (lihat docblock `JadwalKelasController::index()`: flat awal -> grid lama (per Ruangan/Pengajar) -> flat lagi (revisi 7 September 2026 di atas) -> **grid BARU ini**).** Permintaan eksplisit user: "untuk ruangan di buat per tab ya dan di dalam tab sama isi nya seperti skrg yang sudah ada, tampilkan dari jam buka sampe jam tutup dan sesuai dengan jam pengajar, karena page ini yang akan di pantau terus oleh admin" -- lalu dikonfirmasi lewat contoh gambar mockup Excel yang dikirim user (header "Hari Senin 07 September 2026 | Ruangan A", kolom No/Nama Pengajar/Bidang-Kelas/Category/Nama Murid/Mulai/Selesai/Status/Keterangan/Aksi, baris tiap 30 menit dari jam buka sampai tutup, baris jam istirahat ditandai merah kosong).
    - **Sengaja TIDAK menambah langkah pilih/assign Ruangan di alur Tambah/Edit Student** -- ditanya eksplisit lewat AskUserQuestion, jawaban user: "tidak perlu edit ruangan ini lbh ke tampilkan yang slealu di pantau oleh admin". Jadi fitur ini MURNI tampilan (read-mostly, kecuali tombol "+" tambah sesi cepat per slot kosong yang mengarah ke form Tambah Jadwal Kelas manual yang sudah ada, bukan alur baru). Semua sesi auto-generate dari checklist Student (lihat `JadwalStudentController::createRutinFromSlots()`) SELAMA INI selalu `jadwal_ruangan_id = null` -- sesi begini otomatis masuk tab **"Tanpa Ruangan"** tersendiri di akhir daftar tab (cuma muncul kalau memang ada datanya), bukan hilang diam-diam. Admin tetap bisa isi Ruangan manual lewat Edit kalau perlu (field itu sudah ada dari dulu, tidak berubah oleh update ini).
    - **`buildTimeSlots(JadwalBranchSetting)`** (baru, `JadwalKelasController`) -- pecah `jam_buka`..`jam_tutup` branch jadi list slot tetap `durasi_sesi_default_menit` (pola sama dengan `JadwalStudentController::splitSlotIntoChunks()`, sisa di ujung yang kurang dari satu durasi penuh dibuang), tiap slot ditandai `istirahat` (bool) kalau tumpang tindih `jam_istirahat_mulai`/`jam_istirahat_selesai`. Kosong (tidak dipanggil sama sekali) kalau branch belum punya `JadwalBranchSetting`, ATAU tanggal yang difilter bukan hari operasional branch itu (`isHariOperasional()`) -- ditandai jelas lewat alert warning di atas grid, bukan silently kosong tanpa penjelasan.
    - **`buildRuanganGrid(Collection $timeSlots, Collection $sesiList): array`** (baru) -- mengembalikan `rows` (satu per slot waktu, tiap slot dapat properti `sesi` = Collection `JadwalKelas` yang jam mulainya jatuh dalam rentang slot itu) DAN `unmatched` (sesi yang TIDAK ke-plot ke slot manapun -- `start_time` null, atau di luar rentang `$timeSlots`, termasuk kalau `$timeSlots` kosong karena branch/tanggal tidak operasional). **`unmatched` ini murni safeguard tambahan dari sisi implementasi (bukan diminta verbatim oleh user)** -- supaya tidak ada satupun `JadwalKelas` yang diam-diam tidak kelihatan gara-gara redesign grid ini; ditampilkan sebagai daftar "Sesi Tanpa Jam / Di Luar Jam Operasional" di ATAS grid tiap tab (badge kuning di label tab kalau ada isinya).
    - `index()`: `date` SEKARANG SELALU ke-isi (default hari ini kalau kosong di query string) -- beda dari versi tabel flat sebelumnya yang bisa dikosongkan untuk "semua tanggal" (konsep itu tidak berlaku lagi, grid butuh satu sumbu waktu). Filter Pengajar/Mata Pelajaran tetap ada (cuma MENYARING isi grid, baris di luar filter jadi kosong -- bukan menghilangkan slotnya). Filter Branch BARU (dropdown, cuma muncul kalau company punya >1 branch DAN admin tidak locked ke satu branch lewat `ResolvesCompanyContext::isLockedToBranch()`) -- wajib ada satu branch aktif supaya jelas Jam Operasional yang mana dipakai, default ke branch pertama kalau belum pernah pilih. **Pagination dihapus** (grid dibatasi jam_buka-jam_tutup 1 hari, jumlah baris wajar tanpa perlu di-page).
    - View: markup baris sesi (Pengajar/Bidang/Kategori/Murid/Mulai/Selesai/Kehadiran-dengan-form-absensi-inline/Status/Aksi -- PERSIS sama dengan tabel flat sebelumnya, cuma kolom Ruangan dihapus karena sekarang jadi tab pembungkus) di-extract ke partial baru `jadwal-kelas/_sesi-row.blade.php` (isi `<td>` saja, TANPA `<tr>` pembungkus sendiri -- pemanggil yang bungkus `<tr>` supaya bisa nambah kolom "No" di depan tanpa duplikasi markup) supaya SATU sumber dipakai di grid maupun daftar "Sesi Tanpa Jam". `index.blade.php` menampilkan Bootstrap nav-tabs (client-side, satu per tab Ruangan termasuk "Tanpa Ruangan" kalau ada) -- tiap tab-pane: daftar `unmatched` (kalau ada) lalu tabel grid (baris istirahat `table-danger` tanpa data, baris kosong tampil jam slot + tombol "+" cepat ke `jadwal.kelas.create` dengan `start_time`/`ruangan_id`/filter yang relevan di-prefill, baris terisi lewat `_sesi-row.blade.php`).
  - **Update 4 September 2026 (masih sesi yang sama, permintaan baru user setelah lihat grid Jadwal Kelas: "tolong tambahkan kolom ruangan pada edit student ya dan add student juga pasti nya").** Sebelumnya Ruangan cuma bisa diisi manual lewat menu Jadwal Rutin (`jadwal.rutin.edit`) -- jalur checklist Tambah/Edit Student SELALU membuat `JadwalRutin` dengan `jadwal_ruangan_id = null` (lihat update grid Jadwal Kelas di atas soal tab "Tanpa Ruangan"). Sekarang ada dropdown **"Ruangan (opsional)"** baru di `_form.blade.php` (satu sumber dipakai `create.blade.php` DAN `edit.blade.php`, diletakkan setelah field Pengajar) -- SATU pilihan Ruangan berlaku untuk SEMUA jadwal murid itu (bukan per Kategori/tab, konsisten dengan cara field Pengajar/Nama/Status lain di form ini sudah berlaku ke seluruh murid, bukan per-Kategori).
    - `JadwalStudentController::formData()` nambah `ruangans` (`App\Models\JadwalRuangan` aktif, di-scope ke branch yang sama seperti `branchOffices`/`teamMembers` di situ -- kalau company di-lock ke satu branch cuma Ruangan branch itu, kalau tidak semua Ruangan aktif company, label dropdown-nya menyertakan nama branch kalau company >1 branch supaya tidak ambigu).
    - **App\Models\JadwalStudent TIDAK dapat kolom baru** (sama prinsip dengan Kategori, lihat update tampilan Kategori index Student di atas) -- nilai Ruangan "sekarang" di Edit Student DI-DERIVE dari `JadwalRutin` AKTIF milik murid itu (`edit()` query `distinct()->pluck('jadwal_ruangan_id')`): satu nilai unik → di-preselect; kosong → tidak ada yang dipilih (default "- Tanpa Ruangan -"); LEBIH DARI SATU nilai berbeda (kemungkinan data lama sebelum fitur ini ada, murid dengan Kategori berbeda yang kebetulan pernah diisi manual beda ruangan lewat menu Jadwal Rutin) → tidak ada yang di-preselect + alert warning "Murid ini punya lebih dari satu Ruangan berbeda..." supaya admin sadar & menyeragamkan lewat Simpan, bukan diam-diam ketimpa salah satu.
    - **`createRutinFromSlots()` dapat parameter baru `?string $ruanganId`** -- diisi ke `jadwal_ruangan_id` baris `JadwalRutin` yang dibuat (sebelumnya hardcode `null`), dipakai `store()` (Tambah Student) MAUPUN `update()` (Edit Student, slot BARU yang dicentang). Ditambah **pengecekan bentrok Ruangan** (bukan cuma Pengajar seperti sebelumnya) lewat `JadwalRutinConflictService::findRuanganConflict()` (wrapper publik baru, pasangan `findPengajarConflict()` yang sudah ada, prinsip sama dengan pengecekan Ruangan di `JadwalRutinController` untuk jalur manual: "1 kelas 1 ruangan 1 guru = private") -- chunk yang Ruangannya sudah kepakai murid LAIN di jam yang bentrok dilewati (masuk pesan skip), bukan otomatis menimpa.
    - **`update()` juga me-reconcile Ruangan untuk baris `JadwalRutin` aktif yang TETAP DIPERTAHANKAN** (bukan baru dibuat, bukan di-uncheck) -- supaya ganti Ruangan murid tidak perlu uncheck-lalu-centang-ulang semua slotnya satu-satu: tiap baris aktif murid+pengajar itu yang Ruangan-nya beda dari pilihan dropdown saat ini di-update ke Ruangan baru, DENGAN pengecekan bentrok per baris juga (`ignoreId` = baris itu sendiri) -- baris yang Ruangan barunya ternyata sudah kepakai murid lain di jam yang sama DILEWATI (Ruangan lamanya tetap, dilaporkan lewat flash message "Ruangan tidak bisa diubah untuk: ..."), bukan dipaksa pindah. Jumlah yang berhasil diupdate dilaporkan lewat "Ruangan diperbarui untuk N jadwal murid ini.".
    - Validasi baru `jadwal_ruangan_id` di `JadwalStudentController::validator()` -- nullable, company-scoped exists check, pola persis sama dengan `branch_office_id` di validator yang sama & `JadwalRutinController::validator()` untuk field yang sama di jalur manual.
    - **Bug fix 4 September 2026 (masih sesi yang sama, laporan user setelah tes: "murid yang sudah mengikuti registrasi dari step awal sampe ada ruangan tapi terbacanya di jadwal kelas tanpa ruangan").** Draft PERTAMA poin di atas (SUDAH DIKOREKSI, bukan lagi perilaku final) sengaja TIDAK memindah Ruangan sesi (`JadwalKelas`) yang sudah ter-generate bulan ini ketika Ruangan murid diganti lewat Edit Student -- disamakan begitu saja dengan kebijakan "biarkan apa adanya" yang dipakai untuk perubahan JAM/HARI (lihat revisi checklist di atas). Setelah dites user, ini TERBUKTI salah asumsi: murid yang sudah lama terdaftar lalu di-edit untuk diisi Ruangan-nya tetap terbaca "Tanpa Ruangan" di grid Jadwal Kelas (halaman yang justru "akan di pantau terus oleh admin"), padahal seharusnya sekarang sudah ada Ruangan-nya. **Bedanya dengan kasus jam/hari**: mengubah jam/hari mengubah IDENTITAS jadwal itu sendiri (sesi lama mewakili jadwal yang SUDAH TIDAK BERLAKU lagi, jadi sengaja tidak disentuh -- itu keputusan yang tetap benar, TIDAK diubah oleh fix ini), sedangkan Ruangan MURNI metadata tambahan di atas sesi yang jam/harinya SAMA PERSIS -- dan karena Ruangan baru pertama kali bisa diisi dari jalur checklist ini (sebelumnya SELALU null), tidak ada histori valid yang berisiko "ditimpa", cuma data yang belum pernah terisi yang sekarang dilengkapi. Fix: blok reconciliation `update()` sekarang, SETIAP kali berhasil menyamakan Ruangan satu baris `JadwalRutin`, ikut menyamakan Ruangan semua `JadwalKelas` yang `jadwal_rutin_id`-nya sama (`JadwalKelas::where('jadwal_rutin_id', $row->id)->whereNull('attendance_status')->update(['jadwal_ruangan_id' => $ruanganId])`) -- DIBATASI ke sesi yang `attendance_status`-nya masih kosong (belum diabsen) supaya histori kehadiran yang SUDAH tercatat (kemungkinan kecil tapi tetap dijaga) tidak diam-diam ditimpa Ruangan barunya. Jumlah sesi yang ikut disamakan dilaporkan di flash message ("... N sesi Jadwal Kelas yang belum diabsen ikut disamakan Ruangannya"). `store()` (Tambah Student baru) TIDAK terkena bug ini sama sekali -- Jadwal Rutin & sesinya SAMA-SAMA baru dibuat dalam request yang sama, `JadwalRutinSesiGenerator::generateForRutin()`/`createSesi()` sudah menyalin `jadwal_ruangan_id` sejak awal.
  - **Update 4 September 2026 (masih sesi yang sama, permintaan baru user): "tolong tambahkan fungsi export to excel sesuai dengan filter" dan "fungsi print sesuai dengan filter, pastikan clear area ya dan jenis kertasnya itu landscape ya".** Diterapkan di halaman "Jadwal Kelas" (`jadwal.kelas.index`) -- halaman ini yang jadi konteks seluruh diskusi sejak revisi grid per Ruangan di atas, jadi diasumsikan itu maksud user (bukan Student atau halaman lain); belum dikonfirmasi eksplisit, tolong dikoreksi kalau ternyata maksudnya halaman lain.
    - **"SATU SUMBER" untuk filter, supaya export/print DIJAMIN "sesuai dengan filter" yang sedang aktif di layar, tidak mungkin meleset**: logika resolusi filter (Tanggal/Pengajar/Mata Pelajaran/Branch) yang tadinya inline di `JadwalKelasController::index()` di-extract APA ADANYA (tanpa mengubah perilaku) ke method privat baru `resolveFilteredSesi(Request $request): array`, dipakai BERSAMA oleh `index()` (tampilan grid layar) dan `export()` (Excel) yang baru -- kalau nanti filter berubah/nambah, cukup ubah satu tempat, layar & Excel otomatis ikut sinkron. `index()` sendiri hasil refactor-nya PURELY additive: semua key `view()` yang lama tetap sama persis, cuma nambah 3 key baru (`filterPengajarName`/`filterMataPelajaranName`/`filterBranchName`, dipakai header ringkasan saat cetak).
    - **Export Excel** (tombol "Export ke Excel" baru, route GET baru `jadwal.kelas.export`, method `JadwalKelasController::export()`): pakai package `maatwebsite/excel` yang sudah ada di project (pola sama dengan `App\Exports\Jadwal\JadwalLaporanExport` yang sudah ada sebelumnya), file baru `App\Exports\Jadwal\JadwalKelasExport.php` (dua class: `JadwalKelasExport` bungkus `WithMultipleSheets`, `JadwalKelasRuanganSheet` satu sheet per tab Ruangan yang tampil di layar termasuk "Tanpa Ruangan" kalau ada). Tiap sheet: baris info (Ruangan/Tanggal/Branch/filter Pengajar & Mata Pelajaran kalau ada difilter) di atas, baris kosong, lalu tabel sesi (No/Pengajar/Bidang/Kategori/Murid/Mulai/Selesai/Status Kehadiran/Status) -- kolom & urutan sama dengan yang tampil di grid layar, cuma tanpa kolom Aksi (tidak relevan di Excel). Nama sheet disaring dari karakter terlarang Excel (`: \ / ? * [ ]`) & dipotong 31 karakter (nama Ruangan bebas isian admin).
    - **Print** (tombol "Cetak" baru, `onclick="window.print()"` -- murni browser-native, TIDAK ada library PDF baru; ini pola cetak PERTAMA di seluruh aplikasi, jadi jadi rujukan kalau ada halaman lain yang butuh cetak nanti): CSS `@media print` diletakkan di `<style>` scoped di dalam `index.blade.php` sendiri (bukan di `layouts.dashboard`), supaya perubahan cetak ini TIDAK mempengaruhi halaman lain sama sekali. Isinya: `@page { size: landscape; margin: 10mm; }` (permintaan eksplisit user, kertas landscape), sembunyikan header/sidebar/sidebar-backdrop/footer/breadcrumb aplikasi (`#appHeader`/`#sidebar`/`#sidebar-backdrop`/`.footer`/`.main-breadcrumb`) + hilangkan margin/padding wrapper + hilangkan border/shadow card (permintaan "pastikan clear area ya" -- area cetak bersih, cuma tabel jadwal yang tersisa), paksa semua `.tab-pane` (Bootstrap nav-tabs) tampil SEKALIGUS (`display: block !important`, bukan cuma tab aktif) dengan `break-before: page` di antara tiap Ruangan supaya tiap Ruangan cetak di halaman terpisah. Elemen interaktif yang tidak relevan di kertas disembunyikan lewat utility class Bootstrap `d-print-none` (form filter, alert sesi/error, nav-tabs itu sendiri karena semua pane sudah dipaksa tampil, tombol Export/Cetak sendiri, form absensi inline + kolom Aksi di tiap baris sesi lewat `_sesi-row.blade.php`) dan diganti versi `d-none d-print-block` (teks polos) kalau perlu tetap kebaca di kertas: status kehadiran (pola `match()` yang sama dengan Excel) menggantikan form dropdown absensi, header ringkasan judul+tanggal+filter aktif menggantikan form filter, nama Ruangan per tab (karena nav-tabs-nya disembunyikan) ditulis ulang sebagai `<h5>` di awal tiap `tab-pane`.
  - **Update 4 September 2026 (masih sesi yang sama, permintaan baru user): "perbaiki fungsi edit nya ya dari jadwal kelas, itu masih yang lama, di buat modern popup saja. jadi pilih dulu kelas dan category nya, pilih pengajar nya baru tampilkan jam pengajarnya berupa tab seperti sblm sblmnya" + "pastikan popup clean dan bekerja dengan baik, terutama ketika fungsi backspace ... ya nama nya juga admin di jaga jaga saja tapi tampilannya tetap user friendly ya".** Edit Jadwal Kelas sebelumnya cuma halaman penuh (`jadwal.kelas.edit`) dengan dropdown Pengajar bebas + 2 field `datetime-local` mentah (admin ketik tanggal & jam sendiri, tidak tahu jam ketersediaan Pengajar itu apa, dan field Kategori bahkan tidak ada sama sekali di form ini walau kolomnya sudah lama ada di skema `jadwal_kelas`).
    - **Popup, bukan halaman penuh**: tombol pensil tiap baris di grid `jadwal.kelas.index` sekarang membuka `#editKelasModal` (Bootstrap modal) di tempat, tidak pindah halaman lagi. SATU modal dipakai ULANG untuk semua baris (bukan satu modal per baris seperti pola `rosterModal{id}` yang sudah ada di `jadwal-mata-pelajaran/index.blade.php`) -- grid Jadwal Kelas bisa berisi puluhan baris sesi sekaligus (apalagi sejak CSS cetak di atas memaksa semua tab Ruangan ikut ada di HTML), jadi satu modal + dropdown yang cuma di-render sekali menghindari HTML halaman membengkak. Tombol pensil tiap baris cuma bawa `data-kelas` (JSON kecil, nilai field kelas itu SAJA) yang dibaca script buat mengisi modal saat dibuka -- lihat `_sesi-row.blade.php`.
    - **Alur baru "pilih dulu kelas dan category nya, pilih pengajar nya baru tampilkan jam pengajarnya berupa tab"**: (1) dropdown Mata Pelajaran / Bidang, (2) dropdown Kategori ke-filter otomatis ke Kategori milik Mata Pelajaran yang dipilih (kosongkan Mata Pelajaran = semua Kategori company tampil), (3) dropdown Pengajar ke-filter ke Pengajar yang ditugaskan ke Kategori itu (`App\Models\JadwalPengajarKategori`, fallback ke SEMUA Pengajar company kalau Kategori belum ditugaskan siapa pun -- supaya tidak mengunci admin), (4) begitu Pengajar dipilih, panel "Jam Pengajar" tampil sebagai TAB per hari (pola visual PERSIS sama dengan `jadwal-student/_kategori-tabs.blade.php` + `_slot-checklist.blade.php` yang sudah ada) berisi jam yang pengajar itu daftarkan untuk Kategori itu, dipecah per durasi sesi default branch Mata Pelajarannya. Klik satu jam otomatis mengisi Tanggal/Waktu Mulai/Waktu Selesai (field-field itu TETAP bisa diisi manual, tidak dikunci -- popup ini murni kemudahan pengisian). Memilih jam di tab hari lain menggeser Tanggal MAJU (satu arah, tidak pernah mundur -- predictable) ke tanggal terdekat yang hari-nya cocok.
    - **Data (Kategori + peta Pengajar-per-Kategori + jam ketersediaan, method baru `JadwalKelasController::editModalData()` + `splitJamIntoChunks()`) di-embed SEKALI di HTML `index()`**, bukan AJAX per klik admin buka popup, supaya popup terasa instan. `splitJamIntoChunks()` algoritmanya SAMA PERSIS dengan `JadwalStudentController::splitSlotIntoChunks()` tapi SENGAJA diduplikasi (bukan di-extract ke service bersama) supaya perubahan popup ini TIDAK menyentuh file `JadwalStudentController.php` sama sekali, sesuai instruksi user berulang kali sesi ini soal jangan sampai kena fungsi lain. TIDAK menambah pengecekan bentrok (beda dari `JadwalRutinConflictService` yang dipakai alur `JadwalRutin` mingguan) -- `JadwalKelas` di sini satu sesi bertanggal PASTI, butuh desain bentrok yang beda (lawan `JadwalKelas` lain persis di tanggal itu), di luar permintaan kali ini.
    - **`jadwal_kategori_id` sekarang benar-benar diproses**: field ini sudah lama ada di `$fillable` tapi `store()`/`update()`/`validator()` TIDAK PERNAH memvalidasi atau menyimpannya dari form manual -- sekarang divalidasi (nullable, company-scoped, pola sama dengan `jadwal_ruangan_id`) dan disimpan di `store()` & `update()`. Alur sesi PENGGANTI (izin/sakit) tidak berubah -- snapshot-nya tetap menang lewat `array_merge()`.
    - **Validasi gagal balik ke popup, bukan ke halaman lama**: `update()` SEBELUMNYA redirect ke `jadwal.kelas.edit` kalau validasi gagal -- sekarang balik ke `jadwal.kelas.index` (filter yang relevan dengan sesi itu) dengan flash `reopenEditKelasId`, dibaca `index.blade.php`'s script untuk otomatis buka ULANG modal yang sama + isi ulang dari `old()` supaya isian admin yang gagal disimpan tidak hilang. Halaman `jadwal.kelas.edit` (route/controller/view) SENGAJA DIBIARKAN APA ADANYA sebagai fallback kalau URL-nya diakses langsung -- tidak dihapus, sesuai kehati-hatian scope komit sesi ini.
    - **Guard Backspace** (permintaan eksplisit user, "terutama ketika fungsi backspace ... admin di jaga jaga saja"): selagi modal ini terbuka, tombol Backspace yang ditekan waktu fokus BUKAN di kotak isian (input/textarea/select) di-`preventDefault()` -- mencegah jebakan umum browser yang menavigasi ke halaman sebelumnya kalau Backspace ditekan di luar field, yang bisa membuat admin tidak sadar keluar dari popup dan kehilangan isian yang belum disimpan. Guard dipasang saat `shown.bs.modal` & dilepas saat `hidden.bs.modal`, jadi tidak mengganggu Backspace di halaman lain atau saat modal tertutup.
    - **Bug fix 4 September 2026 (masih sesi yang sama, laporan user lewat screenshot: "terjadi error sepertiny aada salah penutup ya", Laravel `ParseError: unexpected token "endif"` di `index.blade.php:427`).** Penyebab: baris opsi dropdown Murid ditulis dengan direktif Blade yang SALING NEMPEL tanpa spasi (`{{ $s->name }}@if(...)...@endif@if(...)...@endif`) -- Blade compiler gagal parse kombinasi begini (BEDA dari ketidakseimbangan jumlah `@if`/`@endif`, jadi lolos dari `php -l` MAUPUN script `blade_balance_check.py` sesi ini yang cuma cek total direktif). Fix: kasih spasi di tiap batas direktif/mustache, disamakan dengan pola yang sudah terbukti jalan di `_form.blade.php` untuk label serupa.
    - **Bug fix 4 September 2026 (masih sesi yang sama, laporan user: "tolong di cek kembali icon pensil nya tidak bisa diklik").** Handler klik tombol pensil (`.js-edit-kelas-btn`) MEMANG mengisi data ke modal lewat `fillModal()`, tapi TIDAK PERNAH memanggil `bootstrap.Modal.getOrCreateInstance(modalEl).show()` -- jadi modal-nya tidak pernah benar-benar tampil walau data-nya sudah terisi di baliknya. Fix: tambah pemanggilan `.show()` setelah `fillModal()`, SENGAJA tetap tidak pakai `data-bs-toggle="modal"` bawaan Bootstrap di tombolnya supaya urutan terjamin (data harus terisi DULU sebelum modal tampil, admin tidak sempat lihat form kosong sekejap).
    - **Revisi besar 7 September 2026 (masih sesi yang sama, permintaan baru user setelah popup pertama di atas jalan): "yang harus di modifikasi adalah tampilkan seluruh pengajar dan jam pengajar yang available atau yang kosong. seperti di student. triger pertama kali itu pilih pengajar, ketika pengajar itu dipiih baru keluarkan bidang dan category di lanjut dengan jam pelajaran yang available. mngkn murid itu mau pindah pengajar dan pindah kelas juga jadi, 1 rangkaian pindah".** Urutan pemicu cascading DIBALIK dari versi pertama: **Pengajar sekarang paling atas** (dropdown isinya SEMUA pengajar yang punya penugasan Kategori aktif) -> begitu dipilih, dropdown **Bidang & Kategori** ke-filter ke yang diajarkan pengajar itu (kebalikan arah dari peta versi pertama) -> begitu Kategori dipilih, panel **Jam** baru tampil. `JadwalKelasController::editModalData()` di-restruktur total: `kategoriPengajarMap`/`pengajarSlotMap` (index Kategori->Pengajar) diganti `pengajarBidangMap`/`pengajarKategoriMap`/`pengajarSlotMap` (index Pengajar->...) + `allPengajars`. View: fungsi JS `rebuildKategoriOptions()`/`rebuildPengajarOptions()` diganti `rebuildBidangOptions()`/`rebuildKategoriOptionsForPengajar()` (arah filter kebalik), `slotsFor(pengajarId, kategoriId)` argumennya ditukar.
      - **Badge Kosong/Terisi tiap jam** (permintaan "tampilkan ... yang available atau yang kosong seperti di student", pola visual dari `jadwal-student/_slot-checkbox.blade.php`'s badge `taken`/`mine`): dihitung `findConflict()` (JS) lawan `bookedSlots` (data baru `editModalData()`, di-query dari `JadwalKelas` LAIN yang jam mulainya jatuh di TANGGAL yang lagi difilter admin di grid -- `$carbonDate`/`bookedSlotsDate`) -- data di-embed statis SEKALI (bukan AJAX), jadi cuma akurat untuk tanggal itu; badge otomatis disembunyikan (jam kembali bisa diklik semua) kalau admin geser field Tanggal di popup ke tanggal lain. Sesi yang lagi diedit sendiri dikecualikan dari hitungan bentrok (`currentExcludeKelasId`, diisi dari `data-kelas.id` tombol pensil ATAU `reopenEditKelasId` session flash -- sempat kelewatan di alur reopen-gagal-validasi, sudah ditambahkan `id` ke `window.JADWAL_KELAS_REOPEN_EDIT`).
      - **Revisi lagi (masih di pesan yang sama, ditegaskan user): "harus nya yg terisi itu tidak bisa diklik".** Awalnya badge Terisi MURNI info visual (chip tetap bisa diklik) -- direvisi jadi chip "Terisi" di-render dengan atribut HTML `disabled` (benar-benar tidak bisa diklik), TETAP BUKAN validasi server (kalau jam bentrok itu diisi manual lewat field Waktu Mulai/Selesai atau lewat request langsung, tetap lolos -- proteksinya murni kemudahan UI popup, bukan aturan bisnis baru, konsisten dengan keputusan "tidak menambah pengecekan bentrok" di popup pertama).
      - **Layout: "dan kolom deskripsi kepotong. catatanya dekat jam pengajar di singkat aja jadi 1 baris atau pop nya di besarkan".** Teks bantuan panjang di bawah panel Jam (3 kalimat + badge HTML inline) DIPERSINGKAT jadi 1 kalimat pendek, DAN modal diperbesar dari `modal-lg` ke `modal-xl` (dua-duanya sekaligus, bukan pilih salah satu, supaya field Deskripsi di bawahnya tidak lagi kedorong/kepotong).
  - **Update 7 September 2026 (masih sesi yang sama, dari screenshot halaman "Pengaturan Pengingat Jadwal"): "kirim berapa lama sblmnya itu di buat add row otomatis ... kita siapan fitur biar admin set sendiri mngkn mau ditambahkan 1 hari sblm nya 6 jam sblmnya" + "kirim rekap tambahkan jam, jam brp mau dikirim ketika di slide tampilkan jam brp mau dikirim" + "tolong di pastikan fitur ini berfungsi kirim ke pengajar, student dan orang tua dan pengingat dan juga request perubahan jadwalnya".** Tiga bagian:
    - **Fitur 1 -- multi waktu pengingat.** `App\Models\JadwalReminderSetting` sebelumnya cuma punya SATU pasang `remind_value`/`remind_unit` (satu company = satu waktu pengingat saja). Tabel baru `jadwal_reminder_rules` (model `App\Models\JadwalReminderRule`, relasi `JadwalReminderSetting::rules()` hasMany) memisahkannya jadi baris-baris independen -- admin bisa klik "+ Tambah Waktu Pengingat" di `jadwal/settings/edit.blade.php` untuk set BEBERAPA waktu sekaligus (mis. "1 hari sebelumnya" DAN "6 jam sebelumnya"), tiap baris jadi pengingat WA terpisah. Kolom lama `remind_value`/`remind_unit` di `jadwal_reminder_settings` SENGAJA TIDAK DIHAPUS (data historis/fallback, migration `create_jadwal_reminder_rules_table.php` mem-backfill jadi baris rule pertama per company supaya company existing tidak kehilangan pengingatnya). `jadwal_kelas_reminder_logs` unique key berubah dari `jadwal_kelas_id` saja jadi komposit `(jadwal_kelas_id, jadwal_reminder_rule_id)` (kolom baru `jadwal_reminder_rule_id`, nullable + `nullOnDelete()` supaya log historis tidak ikut hilang kalau rule-nya dihapus/diganti admin) -- `App\Models\JadwalKelas::reminderLog()` (hasOne) diganti `reminderLogs()` (hasMany). `App\Console\Commands\DispatchDueJadwalReminders` sekarang loop `$setting->rules()`, klaim & dispatch `App\Jobs\SendJadwalReminder` PER rule (constructor job dapat parameter baru `$reminderRuleId`, key `WithoutOverlapping` ikut menyertakan rule id). `App\Http\Controllers\Jadwal\JadwalReminderSettingController::syncReminderRules()` (baru) mencocokkan baris rule dari form ke rule yang sudah ada MILIK SETTING ITU SAJA (update di tempat kalau id cocok, create kalau baru, delete kalau dihapus admin di form) -- id rule company lain tidak bisa "dibajak" lewat form. Maksimal 5 baris rule, ditolak kalau ada pasangan value+unit yang duplikat.
    - **Fitur 2 -- jam kirim rekap pengajar bisa diatur.** Jam kirim rekap H-1 ke pengajar SEBELUMNYA hardcode `19:00` global untuk SEMUA company langsung di scheduler `bootstrap/app.php` (melanggar prinsip section 0 "semua harus via setting, tidak ada yang hardcode"). Kolom baru `remind_notify_pengajar_time` (string `H:i`, default `'19:00'` -- sama persis nilai hardcode lama, company existing tidak berubah perilaku sampai admin sengaja mengubahnya) di `jadwal_reminder_settings`, field time picker baru "Jam Kirim Rekap" di halaman Pengaturan Pengingat (cuma tampil kalau toggle "Kirim rekap H-1 otomatis ke pengajar" nyala, lewat JS show/hide). Scheduler `jadwal:dispatch-pengajar-reminders` diubah dari `->dailyAt('19:00')` jadi `->everyFifteenMinutes()` -- `App\Console\Commands\DispatchJadwalPengajarDailyReminders` sendiri yang menyaring, cuma memproses company yang jam sekarang (Asia/Jakarta) sudah lewat `remind_notify_pengajar_time` miliknya; idempotensi log yang sudah ada (unique `pengajar_id`+`reminder_date`) mencegah rekap dobel walau command jalan berkali-kali sehari setelah jam settingnya lewat.
    - **Fitur 3 -- audit sistem notifikasi ("sblm banyak perubahan fiturnya sudah berjalan dengan baik ... tolong di pastikan lagi berjalan dan sesuai dengan no yang di database").** Diverifikasi manual (baca kode, TIDAK bisa query DB langsung -- `device_bash` sandbox tidak punya binary `php`/`mysql`, lihat catatan di bawah): (1) pengingat student/orang tua (`SendJadwalReminder`) baca `$kelas->student->parent_phone_number`/`student_phone_number` sesuai `remind_target` -- benar & TIDAK berubah oleh revisi Edit Jadwal Kelas popup manapun sesi ini. (2) rekap pengajar (`SendJadwalPengajarReminder`) baca `User::find($pengajarId)->handphone` -- benar, tidak tersentuh. (3) notifikasi reschedule (`JadwalRescheduleRequestController::sendRescheduleNotifications()`) baca `$reschedule->requester_phone`, `$reschedule->jadwalStudent?->pengajar?->handphone`, `$owner->handphone` -- benar, tidak tersentuh. (4) `JadwalKelasController::update()` MASIH memanggil `notifyPengajarScheduleChanged()` kalau jam/pengajar berubah (snapshot old value sebelum `update()`, cek `$timeChanged`/`$pengajarChanged`) -- dicek ulang eksplisit karena banyak perubahan di popup Edit sesi ini, TIDAK ADA REGRESI. Kesimpulan: tidak ditemukan fungsi yang rusak dari perubahan-perubahan sesi ini.
    - **PENTING -- belum bisa diverifikasi jalan di database sungguhan dari sesi ini**: 3 migration baru (`create_jadwal_reminder_rules_table`, `add_reminder_rule_to_jadwal_kelas_reminder_logs_table`, `add_pengajar_reminder_time_to_jadwal_reminder_settings_table`) HARUS dijalankan (`php artisan migrate`) di server/device sungguhan user, lingkungan kerja sesi ini tidak punya akses `php`/`mysql` untuk menjalankannya sendiri. Semua migration idempoten (`Schema::hasColumn()`/`hasTable()` guard) jadi aman kalau sempat gagal di tengah jalan dan perlu di-retry.
    - **Bug fix 4 September 2026 (laporan user lewat error nyata pas `php artisan migrate` di device sungguhan): "SQLSTATE[HY000]: General error: 1553 Cannot drop index 'jadwal_kelas_reminder_logs_jadwal_kelas_id_unique': needed in a foreign key constraint".** Migration `add_reminder_rule_to_jadwal_kelas_reminder_logs_table.php` punya DUA bug, keduanya diperbaiki: (1) **Urutan salah** -- versi awal drop unique `jadwal_kelas_id` DULU baru pasang unique komposit baru. MySQL/InnoDB menolak (unique `jadwal_kelas_id` itu satu-satunya index yang menopang FK ke `jadwal_kelas`, dan FK WAJIB selalu punya index pendukung, tidak boleh kosong walau cuma sesaat) -- fix: pasang unique komposit BARU dulu (kolom pertamanya `jadwal_kelas_id`, otomatis bisa menopang FK yang sama), BARU drop unique lama. (2) **`try`/`catch` yang dipakai untuk idempotensi TIDAK PERNAH benar-benar menangkap apa pun** -- `$table->dropUnique()`/`$table->unique()` di dalam closure `Schema::table()` cuma MENGANTRE command ke Blueprint, SQL sungguhan (dan exception-nya kalau gagal) baru dieksekusi Laravel SETELAH closure selesai, di luar jangkauan `try`/`catch` yang dibungkus di dalam closure -- persis kenapa error di atas lolos sampai ke terminal user, bukannya ketangkap "sudah pernah jalan, lanjut" seperti maksudnya. Fix: ganti ke `Schema::hasIndex($table, $columnsOrName, $type)` (API resmi Laravel 12, sudah diverifikasi ada di `vendor/laravel/framework` project user) -- query index-nya DULU sebelum memutuskan apakah command perlu diantre sama sekali, prinsip sama dengan `Schema::hasColumn()` yang sudah dipakai migration lain di project ini. **Koreksi klaim di komentar migration sebelumnya**: docblock migration ini SEBELUM fix ini mengklaim polanya "sama dengan migration reschedule notification settings" -- klaim itu SALAH, sudah dicek ulang: migration reschedule notification settings ITU SENDIRI sebenarnya pakai `Schema::hasColumn()` guard (persis pola yang benar), BUKAN `try`/`catch` di dalam closure sama sekali -- jadi bug (2) di atas murni salah tulis di migration ini sendiri, bukan pola yang diwariskan dari migration lain. **Pelajaran umum buat migration lain di project ini**: `try`/`catch` DI DALAM closure `Schema::table()` tidak pernah valid untuk idempotensi command Blueprint apa pun (kolom, index, maupun FK) -- selalu pakai `Schema::hasColumn()`/`Schema::hasIndex()`/`Schema::hasTable()` (query DULU sebelum memutuskan, BUKAN jalankan-lalu-tangkap) seperti migration reschedule notification settings yang sudah benar sejak awal.

Tujuan besar project ini: menjadikan Konexa sebagai **CRM + tools marketing berbasis WhatsApp yang andal dan mudah dipakai** — bukan aplikasi dengan banyak menu terpisah yang membingungkan. Prinsip "kemudahan, tidak terlalu banyak menu" ini jadi acuan setiap kali mendesain fitur baru atau menata ulang UI (lihat juga section 5 & 7.3 soal konsolidasi UI).

Arah konkret yang disepakati:

1. **Satu menu untuk semua jenis pengiriman pesan** — form "pengaturan pengiriman" menggabungkan apa yang sekarang terpisah jadi "pesan terjadwal" dan "chatbot flow" (plus jenis sejenis lain: broadcast, sekali kirim, berulang, bertahap dengan interval) jadi SATU form — tinggal pilih jenisnya di dalam form itu, bukan halaman/menu yang beda-beda per jenis. **Arah refactor baru** — cek dulu kondisi menu/controller aktual sebelum mulai kerja, jangan asumsi sudah begini.
2. **Satu menu untuk semua jenis balasan pesan** — pengaturan balas pesan (AI, chatbot flow, auto-reply, balasan cepat) digabung jadi satu menu, semua jenis balasan diatur dari situ. **Arah refactor baru**, sama seperti poin 1.
3. **Phone book export & import** — ✅ sudah ada di kode (`PhoneBookController::export()`, `importTemplate()`, `import()`, diverifikasi 21 Agustus 2026). Tidak perlu dibangun ulang, cukup dipastikan tetap terjaga kalau area ini disentuh refactor lain.
4. **Template WA** tetap dipertahankan sebagai fitur pendukung pengiriman/balasan pesan — jangan hilang/kesampingkan waktu konsolidasi menu di poin 1 & 2.
5. **Assignment inbox ke rekan kerja atau device lain** — fitur ini WAJIB tunduk ke batasan package yang dipilih company, bukan fitur unlimited untuk semua tier. Cek dulu apakah assignment sekarang sudah gated per package atau belum (lihat juga Known Issues soal 2 sistem assignment yang berjalan sendiri-sendiri).
6. **Begitu package habis, SEMUA layanan berhenti** — bukan cuma pengiriman WhatsApp (yang sudah ditutup sebagian, lihat section 7.1), tapi seluruh fitur yang bergantung pada package aktif (assignment device/kontak lain, dsb) harus konsisten ikut berhenti juga.
7. **Package dibedakan berdasarkan 4 metric**: jumlah pengiriman pesan, jumlah device, jumlah cabang (branch), dan jumlah phone book (kontak) yang boleh ditampung — ini sudah match dengan kerangka `LimitMetric`/`PackageLimit` yang sudah ada (`broadcast_send`, `device_count`, `branch_count`, `contact_count`, lihat section 7.2). Tinggal dipastikan operasional (superadmin isi angkanya per package) dan konsisten dipakai di semua fitur relevan.
8. **Add-on/integrasi pihak ketiga** (Google Form, dan lain-lain ke depannya) — arsitekturnya harus mendukung penambahan integrasi baru tanpa bongkar besar-besaran. `GoogleFormWebhookController` sudah ada sebagai contoh pertama pola integrasi semacam ini.
9. **Broadcast harus tetap aman kalau banyak user jalankan bersamaan** — ini prinsip yang sudah jadi pondasi section 0 dan sudah banyak diverifikasi di section 7 (throttle, quota lock, dsb), tetap jadi syarat mutlak setiap fitur baru yang menyentuh pengiriman WA.

## 0. Prinsip Dasar — WAJIB Dicek Setiap Kali Ada Fitur/Endpoint/Job Baru

Aplikasi ini adalah **SaaS multi-tenant dengan target banyak user dan banyak company memakai sistem secara bersamaan** — bukan aplikasi internal skala kecil. Jangan pernah menganggap sebuah fitur baru "selesai" sebelum 5 pertanyaan ini dijawab:

1. **Race condition** — Kalau kode ini dipanggil 2 request/worker/user secara bersamaan (2 klik submit bebarengan, 2 webhook yang datang hampir bersamaan, 2 cron job yang overlap), apakah hasilnya tetap benar? Kalau menyentuh counter/saldo/kuota/baris yang direbutkan banyak proses, WAJIB pakai `DB::transaction()` + `lockForUpdate()` seperti `PackageLimitService::reserve()`. Kalau soal klaim/dedup pekerjaan, WAJIB pakai unique constraint di database + try/catch `QueryException` seperti `DispatchDueWaMessageSchedules::claimAndDispatch()`. Detail pola lengkap ada di poin 2.
2. **Proses dobel / idempotency** — Kalau request, webhook, atau job yang sama entah kenapa terpanggil 2x (WhatsApp retry kirim webhook, user klik tombol 2x karena koneksi lambat, job di-retry setelah timeout), apakah efeknya tetap sama seperti dipanggil sekali (saldo tidak nambah/kepotong 2x, pesan WA tidak terkirim dobel, baris tidak ke-insert dobel)? Kalau belum, tambahkan idempotency key atau unique constraint yang relevan sebelum fitur dianggap selesai — jangan cuma andalkan "kemungkinannya kecil".
3. **Bottleneck / siap skala besar** — Kalau tabel yang disentuh nanti berisi jutaan baris dan dipakai ratusan company sekaligus, apakah query-nya masih cepat? Cek N+1 (`with(...)`), cek index kolom yang dipakai di `WHERE`/`JOIN`/`ORDER BY`, cek pagination untuk list yang bisa tumbuh besar (chat, log pengiriman, dsb). Detail di poin 3.
4. **Broadcast/pengiriman WA harus tetap jalan bersamaan** — broadcast/auto-reply/AI bot dari company A tidak boleh saling block atau nge-lag broadcast company B yang jalan di saat bersamaan, tapi tetap WAJIB lewat `BroadcastThrottleService` supaya tidak kena ban WhatsApp (jeda/jitter per device tetap dijaga). Detail di poin 4.
5. **Uang/deposit/wallet** — kalau fitur menyentuh saldo/deposit, TIDAK BOLEH pola "cek saldo dulu, baru kurangi" tanpa lock (celah race condition klasik yang bisa bikin saldo minus atau double-spend kalau 2 request datang bersamaan). WAJIB `lockForUpdate()` + transaksi, ikuti pola `PackageLimitService`.
6. **Clean code & tampilan responsive** — kalau fitur ini ada sisi UI (blade baru/blade diubah), sudah pakai komponen Blade yang sudah ada (`x-primary-button`, `x-text-input`, dsb, bukan HTML mentah yang mengulang), sudah extend layout yang ada, dan sudah dicek tampilannya di mobile/tablet/desktop? Kalau ada logic bisnis, sudah dipindah ke Service class (bukan numpuk di controller)? Detail di poin 5.

Kalau salah satu jawabannya "belum aman" atau "belum dicek", jangan lanjut — perbaiki dulu polanya mengikuti contoh yang sudah ada di codebase (poin 1-4 di bawah), baru boleh dianggap selesai.

## 1. Keamanan (Security)

- Setiap endpoint baru WAJIB jelas siapa yang boleh akses: cek middleware auth/otorisasi yang relevan (`superadmin`, `active.package`, `menu.access`, `wa.api-key`, dll — lihat `bootstrap/app.php` untuk daftar lengkap) sebelum menambah route baru.
- Jangan pernah percaya input dari request tanpa validasi (`$request->validate()` di setiap controller, seperti pola yang sudah konsisten dipakai di codebase ini).
- Kalau membuat endpoint yang dipanggil pihak ketiga (bukan user login), ikuti pola `WaApiSendMessageController` — gunakan API key middleware, jangan `auth` biasa, dan batasi kemampuannya seminimal mungkin (jangan expose lebih dari yang benar-benar dibutuhkan pihak ketiga).
- **Setiap kali menambah/mengubah endpoint API pihak ketiga/per-device** (apa pun di bawah prefix `/api/wa-api/...`, atau API sejenis untuk integrasi eksternal ke depannya seperti add-on Google Form dkk), WAJIB catat/update dokumentasinya dalam format **OpenAPI 3.0 JSON** di `docs/api/` (satu file per grup API, contoh: `docs/api/wa-api-v1.openapi.json`) — bukan cuma komentar di kode. Ini supaya dev (internal atau pihak ketiga) bisa lihat dokumentasinya lewat tool standar (Swagger UI, Redoc, Postman import, dsb) tanpa harus baca source code. Dokumentasikan: path, method, header auth yang dipakai, schema request body, dan semua kemungkinan response (sukses maupun error) — contoh lengkap sudah ada di `docs/api/wa-api-v1.openapi.json` untuk endpoint `POST /wa-api/v1/send-message`, ikuti pola yang sama untuk endpoint baru. Update dokumentasi ini LANGSUNG waktu ngerjain fungsinya (bukan sesi terpisah) — tidak perlu dijelaskan panjang lebar dulu ke user, cukup dikerjakan sekalian.
- Jangan log data sensitif (token, password, nomor kartu, dll) — kalau perlu logging untuk debug, mask/redact dulu.
- Setiap kali menambah fitur yang menyentuh uang/wallet/deposit, cek dulu apakah `PackageLimitService` atau pola `lockForUpdate` + unique constraint perlu dipakai supaya tidak ada race condition yang bisa merugikan (lihat poin 2).

## 2. Concurrency & Race Condition

Codebase ini sudah punya pola yang benar untuk ini — WAJIB diikuti setiap kali menambah fitur yang bisa diakses banyak proses/worker bersamaan:

- **Klaim/dedup pekerjaan**: pakai unique constraint di level database (bukan cuma cek `firstOrCreate` di kode PHP tanpa try/catch — lihat `DispatchDueWaMessageSchedules::claimAndDispatch()` untuk contoh pola yang benar: `lockForUpdate()` + try/catch `QueryException`, sama seperti `PackageLimitService::lockOrCreateUsage()`).
- **Counter/kuota yang dipakai bersamaan** (uang, kuota kirim, dsb): ikuti pola `PackageLimitService::reserve()` — transaksi terkunci (`DB::transaction()` + `lockForUpdate()`), plus try/catch `QueryException` untuk race pada baris pertama (lihat `lockOrCreateUsage()`).
- **Job queue yang tidak boleh dobel**: pakai `WithoutOverlapping` middleware seperti di `SendScheduledWaMessage`.
- **Rate limiting per-resource** (device WA, dsb): pakai Laravel `RateLimiter` dengan cache store yang atomik (`database` atau `redis`, JANGAN `file`/`array` untuk sesuatu yang diakses banyak worker — lihat pola `BroadcastThrottleService`).
- Kalau menambah job/fitur baru yang mengirim pesan WA (auto-reply, AI bot, chatbot flow, dsb), WAJIB ikut lewat `BroadcastThrottleService` juga — jangan cuma broadcast terjadwal yang dibatasi. `SendAutoReplyMessage` dan `SendAiBotReply` sudah mengikuti pola ini (`$throttle->attempt($deviceId, $companyId)` sebelum kirim); jadikan keduanya contoh referensi untuk job pengirim WA berikutnya.

## 3. Skalabilitas

- Hindari query N+1 — eager load relasi yang dibutuhkan (`with(...)`), seperti pola yang sudah dipakai di `DealController::index()`.
- Untuk data yang bisa tumbuh besar (chat list, log pengiriman, dsb), selalu pertimbangkan pagination/limit, jangan fetch semua sekaligus.
- Kalau menambah CSS/JS baru untuk halaman besar seperti Inbox, jangan tambah lagi ke file blade yang sudah 150KB+ (`inbox/inbox.blade.php`) — ini sudah jadi masalah performa (tidak bisa di-cache browser). Pertimbangkan file terpisah kalau menyentuh area ini.

## 4. Anti-Ban WhatsApp

- Backend ini pakai koneksi WhatsApp tidak resmi (multi-device pairing via QR, bukan WhatsApp Business Platform resmi) — jadi SEMUA pengiriman pesan otomatis/massal harus tetap hati-hati:
  - Selalu ada jeda/jitter antar pengiriman ke banyak penerima (lihat pola di `DispatchDueWaMessageSchedules`).
  - Selalu cek opt-out (`BroadcastOptOutService`) sebelum mengirim ke nomor manapun secara massal.
  - Jangan kirim konten yang benar-benar identik ke ratusan nomor tanpa variasi kalau bisa dihindari.

## 5. Clean Code & Konsistensi Tampilan (Responsive)

- **PENTING — project ini punya 2 sistem UI berbeda, jangan disamaratakan** (koreksi dari versi sebelumnya di dokumen ini): `layouts.dashboard` (dipakai HAMPIR SEMUA fitur nyata — inbox, CRM, broadcast, package, dsb) pakai template admin **Bootstrap statis** (`public/be/assets/css/bootstrap.min.css`, `app.min.css`, di-load lewat `<link>` biasa, bukan Vite). `layouts.app` (bawaan scaffold Breeze, dipakai untuk halaman auth-adjacent) pakai **Tailwind** lewat `@vite(...)`, dan komponen di `resources/views/components/` (`x-primary-button`, `x-text-input`, dst) itu gaya Tailwind — kemungkinan besar TIDAK relevan/tidak dipakai di halaman dashboard yang sebenarnya. Sebelum bikin form/tombol baru: cek dulu halaman itu extend layout yang mana, baru ikuti gaya markup yang sudah dipakai tetangganya di layout yang sama — jangan campur Tailwind class ke halaman Bootstrap atau sebaliknya.
- **Ikuti layout yang sudah ada** — halaman baru extend `layouts.dashboard` (mayoritas fitur) atau `layouts.app`/`layouts.guest` sesuai konteksnya (bukan bikin `<html>`/`<head>` sendiri dari nol), supaya header/menu/footer (`layouts/partials/header.blade.php`, `menu.blade.php`, `footer.blade.php`) otomatis konsisten dan tidak dobel-maintain.
- **Responsive wajib, ikuti pola breakpoint yang sudah dipakai** — cek bagaimana halaman existing (dashboard, inbox, dsb) mengatur breakpoint Tailwind-nya (`sm:`, `md:`, `lg:`), pakai pola yang sama di halaman baru. Sebelum fitur dianggap selesai, cek tampilannya minimal di 3 lebar: mobile (~375px), tablet (~768px), desktop (~1280px) — jangan cuma dicek di layar besar lalu diasumsikan otomatis rapi di HP.
- **Clean code / best practice umum**:
  - Logic bisnis yang lebih dari beberapa baris jangan ditaruh langsung di controller — pindahkan ke Service class (ikuti pola `PackageLimitService`, `BroadcastThrottleService`) supaya controller tetap tipis dan gampang ditest.
  - Validasi input pakai Form Request class kalau aturannya sudah cukup kompleks/reusable, bukan numpuk `$request->validate([...])` panjang langsung di controller kalau dipakai di lebih dari satu tempat.
  - Penamaan variabel/method/class harus jelas menjelaskan maksudnya (bahasa Inggris untuk kode, konsisten dengan codebase yang sudah ada) — hindari singkatan ambigu.
  - Hindari duplikasi: kalau nulis logic yang mirip dengan yang sudah ada di tempat lain, cek dulu apakah bisa di-extract jadi helper/trait/service yang dipakai bersama, jangan copy-paste.
  - Ikuti code style yang sudah ada di file sekitarnya (indentasi, urutan import, dsb); kalau project ini punya Laravel Pint/PHP-CS-Fixer terpasang, jalankan sebelum commit.

## 6. Alur Kerja Kolaborasi

- **Jangan langsung coding kalau user masih dalam tahap diskusi/investigasi** — tunggu instruksi eksplisit untuk mulai implementasi. Kalau ragu, tanya dulu.
- Untuk perubahan yang menyentuh data production (migration, query manual, dsb), selalu ingatkan soal backup dan konfirmasi nama database yang benar (demo vs production) sebelum eksekusi.
- Jelaskan trade-off keamanan/skalabilitas di akhir setiap perubahan kode yang cukup signifikan — jangan cuma bilang "sudah selesai".
- Bahasa komunikasi: Bahasa Indonesia.

## 7. Alur Branch & Deploy (staging → main)

Project ini pakai 2 branch yang masing-masing terhubung ke 1 environment — **jangan pernah commit langsung ke `main`**, semua perubahan wajib lewat `staging` dulu:

| Branch | Environment | Folder di VPS | Fungsi |
|---|---|---|---|
| `staging` | `staging.konexa.id` | `/var/www/staging.konexa` (DB `konexa_staging`, backend Go di `/var/www/g_backend_staging` port `8082`) | tempat coba/tes sebelum ke production |
| `main` | `app.konexa.id` | `/var/www/app.konexa` (backend Go production di `/var/www/g_backend_konexa`, **belum** systemd — lihat Known Issues) | production, dipakai customer asli |

**Step-by-step tiap ada perubahan kode:**

1. Kerja di lokal (`C:\xampp\htdocs\teleios`), pastikan di branch `staging`: `git checkout staging` (atau `git pull origin staging` dulu kalau branch-nya sudah ada perubahan dari orang lain).
2. Kalau nambah dependency composer baru, jalankan `composer update <nama-package>` (bukan `composer update` polos tanpa nama package, supaya tidak ikut update package lain yang tidak berhubungan) supaya `composer.lock` ikut ter-update dan ikut di-commit.
3. `git add .` — **cek dulu `git status` sebelum ini**, pastikan tidak ada file yang tidak seharusnya ikut (`.env`, folder sampah semacam `_to_delete/`, dsb).
4. `git commit -m "pesan yang jelas"` lalu `git push origin staging`.
5. Di VPS, masuk ke `/var/www/staging.konexa`, jalankan `git pull origin staging`. Kalau ada perubahan `composer.lock`: `composer install`. Kalau ada migration baru: **cek dulu `.env` benar-benar `DB_DATABASE=konexa_staging`**, baru `php artisan migrate`. Kalau ada perubahan asset front-end: `npm run build`. Kalau ada perubahan config/`.env`: `php artisan config:clear`.
6. Test langsung di `https://staging.konexa.id` — pastikan fitur yang diubah benar-benar jalan, bukan cuma asumsi.
7. Kalau sudah oke di staging, baru merge ke `main`: lewat Pull Request di GitHub (lebih aman, bisa direview) — merge `staging` ke `main`, atau `git checkout main && git pull && git merge staging && git push origin main` kalau kerja sendiri tanpa PR.
8. Di VPS, masuk ke `/var/www/app.konexa` (production) — **WAJIB backup database production dulu** dan konfirmasi nama database yang benar sebelum lanjut (lihat section 6). Baru `git pull origin main`, ulangi step composer/migrate/npm/cache seperti di atas tapi untuk production.
9. Kalau ada perubahan job/queue, jalankan `php artisan queue:restart` di production supaya worker yang lagi jalan pakai kode lama ganti ke kode baru.
10. Verifikasi `app.konexa.id` normal setelah deploy.

Kalau perlu mundur (rollback) di production: `git log` cari commit sebelumnya, `git revert <commit-hash-merge>` (bukan `reset --hard`, supaya histori tetap utuh dan aman untuk branch yang sudah di-push), lalu ulangi step 8-10.

## 8. Task Menuju SaaS Siap Pakai (Audit 21 Agustus 2026)

Audit ini menjawab 5 titik yang diminta dicek untuk kesiapan SaaS multi-tenant (broadcast bisa jalan bersamaan banyak user). Kabar baiknya, pondasinya sudah cukup matang di beberapa bagian — tapi ada celah nyata yang perlu ditutup. Ringkasan aksi konkret dari audit ini sudah dipindahkan ke checklist prioritas di paling atas dokumen ini — bagian di bawah ini detail/alasan teknisnya.

### 7.1 Pengiriman WA harus berhenti otomatis begitu layanan/kuota habis

Sudah benar di `SendScheduledWaMessage`, `SendAutoReplyMessage`, `SendAiBotReply` — ketiganya sudah panggil `PackageLimitService::requireActivePackage()` sebelum kirim, dan `SendScheduledWaMessage` juga sudah `reserve()`/`release()` kuota `broadcast_send`.

Yang masih bolong:
- [ ] **`WaApiSendMessageController`** (endpoint API pihak ketiga, `POST` send message pakai `WaApiKey`) **tidak** memanggil `requireActivePackage()`, `reserve()`, atau `BroadcastThrottleService` sama sekali — langsung ke `InboxService::send()`. Pihak ketiga masih bisa terus kirim pesan walau package company sudah expired/kuota habis, dan tanpa jeda/jitter anti-ban. **Ini prioritas paling tinggi untuk ditutup** — lihat spesifikasi refactor lengkap di checklist prioritas poin 2 di atas.
- [ ] `SendAutoReplyMessage` dan `SendAiBotReply` cuma cek package aktif atau tidak, tapi TIDAK `reserve()`/`consume()` kuota apa pun — auto-reply dan balasan AI bot tidak mengurangi kuota `broadcast_send` (atau metric lain), jadi company bisa kirim auto-reply/AI bot tanpa batas walau kuota pesan terjadwalnya sudah habis. Perlu diputuskan: memang sengaja unlimited (kebijakan beda dari broadcast terjadwal), atau perlu metric kuota sendiri (`auto_reply_send`, `ai_bot_send`)?

### 7.2 Pembatasan device, kontak, broadcast, branch

Kerangka `PackageLimitService` + `LimitMetric` sudah generic dan dipakai konsisten: `ConnectDeviceController` (`device_count`), `PhoneBookController` (`contact_count`), `BranchOfficeController` (`branch_count`), `SendScheduledWaMessage` (`broadcast_send`). Kodenya sudah benar.

Yang masih perlu dikerjakan (operasional, bukan kode):
- [ ] Semua metric ini **fail-open** (unlimited) selama superadmin belum benar-benar mengisi `LimitMetric` + `PackageLimit` untuk suatu package. Cek di Superadmin > Package Limit: pastikan setiap package yang dijual sudah punya angka limit untuk `device_count`, `contact_count`, `branch_count`, `broadcast_send` — kalau belum diisi, company pemegang package itu punya kuota tak terbatas tanpa disadari siapa pun.
- [ ] Kalau poin 7.1 soal auto-reply/AI bot mau dibatasi juga, perlu metric key baru didaftarkan di sini juga.

### 7.3 Efisiensi form & refactor menu

- [ ] **Ditemukan 2 sistem UI berjalan bersamaan** (lihat koreksi di section 5) — ini kemungkinan besar sumber form yang tidak efisien/berulang, karena form-form di `layouts.dashboard` (mayoritas app) kemungkinan nulis markup Bootstrap manual berulang-ulang alih-alih pakai component reusable (component yang ada di `resources/views/components/` itu gaya Tailwind, kemungkinan tidak kepakai di sana). Task: putuskan satu sistem UI resmi (kemungkinan tetap Bootstrap admin theme karena itu yang dipakai mayoritas halaman), lalu buat set komponen Blade reusable BERGAYA BOOTSTRAP untuk form-input/button/card yang sering diulang.
- [ ] `resources/views/layouts/partials/menu.blade.php` sudah 738 baris / 48KB — daftar menu ditulis manual sebagai HTML panjang dengan banyak `@if`/`@php` inline untuk cek permission per item. Pertimbangkan refactor jadi data-driven (array/config daftar menu + permission-nya, di-loop lewat 1 partial/component kecil) supaya nambah/hapus menu tidak perlu edit HTML manual yang panjang dan gampang salah taruh permission check.
- [ ] Cek apakah form-form besar lain (CRUD company/user/package di Superadmin, dsb) sudah pakai pola partial `_form.blade.php` yang reusable (contoh yang sudah ada: `chat/message-schedules/_form.blade.php`, `chat/message-templates/_form.blade.php`) — kalau belum, standardisasi ke pola itu.

### 7.4 Keamanan pengiriman saldo, pengisian deposit & pembelian package

Ini bagian **paling matang** dari semua yang diaudit — sudah pakai pola production-grade:
- `WalletLedgerService::move()` — setiap perubahan saldo WAJIB lewat sini, pakai `lockForUpdate()` + `DB::transaction()`, setiap perubahan tercatat immutable di `LedgerEntry` (before/after balance).
- `WalletTransferController` — transfer antar wallet mengunci KEDUA wallet dalam urutan `id` yang konsisten (`orderBy('id')->lockForUpdate()`), mencegah deadlock kalau ada 2 transfer berlawanan arah bersamaan.
- `DuitkuCallbackController` (webhook top-up) — idempotent dengan benar: re-fetch status deposit pakai `lockForUpdate()` DI DALAM transaction sebelum diproses, jadi callback yang dikirim dobel oleh Duitku tidak akan mengkredit saldo 2x. Ada audit log & riwayat status juga.
- **`PackageCheckoutController::store()`** (beli package pakai saldo wallet) — diverifikasi 21 Agustus 2026, juga sudah rapi: pakai `Cache::lock("package-checkout:{user_id}", 15)` non-blocking sebagai guard submit-ganda (double-click/retry tidak akan memotong saldo 2x untuk 1 niat beli), kuota kode promo di-re-check pakai `lockForUpdate()` DI DALAM transaction (supaya kuota promo yang hampir habis tidak bisa "kejual lebih" waktu 2 checkout konkuren), dan pembayarannya sendiri tetap lewat `WalletLedgerService::debit()`. Lock selalu di-release lewat `finally`.
- **`VoucherRedeemController::store()`** (redeem kode aktivasi jadi subscription aktif) — juga diverifikasi 21 Agustus 2026: voucher yang di-redeem DAN voucher aktif sebelumnya (dipakai untuk logic chaining durasi) sama-sama di-`lockForUpdate()` di dalam satu transaction, supaya 2 kali redeem hampir bersamaan tidak menghasilkan window durasi yang overlap/salah hitung.

Task kecil yang masih ditemukan (bukan soal concurrency, soal konsistensi pola):
- [ ] `Superadmin\UserController::reset()` (fitur reset total data user) meng-update `wallet.balance` langsung ke 0 (`$user->wallet->update(['balance' => 0])`), TIDAK lewat `WalletLedgerService` — jadi perubahan saldo ini tidak tercatat sebagai `LedgerEntry` seperti perubahan saldo lainnya. Sudah di dalam `DB::transaction()` jadi aman dari sisi atomicity, tapi melanggar aturan "semua perubahan saldo WAJIB lewat WalletLedgerService" yang dipegang di semua tempat lain. Risiko rendah (ini fitur superadmin-only yang jarang dipakai), tapi sebaiknya diperbaiki untuk konsistensi — ganti jadi panggil `WalletLedgerService::debit()` sejumlah saldo yang ada sebelum di-nolkan.

Selebihnya tidak ada task mendesak — cuma dijaga: setiap fitur uang BARU (refund, komisi referral, dsb) WAJIB lewat `WalletLedgerService`, jangan bikin jalur update saldo baru yang terpisah.

### 7.5 Chatbot: query/update data aplikasi

`AiReplyGenerator` (`app/Services/AiBot/AiReplyGenerator.php`) saat ini murni text-in/text-out — sistem prompt + knowledge base statis (dokumen upload). Belum ada function-calling/tool-use untuk baca/ubah data aplikasi (cek status pesanan, lihat data CRM, update lead, dsb).

Ini task pengembangan baru (bukan bug):
- [ ] Tentukan use case konkret dulu (misal: chatbot jawab "status pesanan saya" via query ke data milik company itu).
- [ ] Tambah kemampuan function-calling di `AiProviderClientResolver`/`AiReplyGenerator` (provider AI modern umumnya sudah support tool/function calling).
- [ ] WAJIB scope ketat semua query/update tool ke `company_id` milik bot itu — jangan sampai chatbot bisa baca/ubah data company lain.
- [ ] Aksi UPDATE oleh chatbot sebaiknya tercatat di audit log (pola `AuditLog` yang sudah dipakai `DuitkuCallbackController`), jangan silent.

## 9. Audit Lanjutan — Teleios + g_backend (27 Agustus 2026)

Audit ini mencakup 4 sudut: concurrency & keamanan uang di Laravel, skalabilitas & isolasi multi-tenant di Laravel, keamanan/concurrency di `g_backend` (Go, belum pernah diaudit sedalam ini sebelumnya), dan titik sambung Laravel↔Go + endpoint pihak ketiga. Semua pola production-grade yang sudah diklaim section 7.1-7.4 (PackageLimitService, WalletLedgerService, BroadcastThrottleService, DispatchDueWaMessageSchedules, checkout/voucher, webhook Duitku) **terverifikasi ulang masih benar, tidak ada regresi**. Isolasi multi-tenant Laravel juga secara umum solid (pola scoping `company_id` konsisten). Temuan baru di bawah, diurutkan dari yang paling berisiko.

### 9.1 `g_backend`: tidak ada panic recovery (Risiko Tinggi)

Tidak ada satupun `recover()` di seluruh codebase Go. Recovery middleware Gin cuma melindungi goroutine per-HTTP-request — goroutine manual (`RestoreSessions`, connection watchdog, `eventHandler` whatsmeow, webhook fire-and-forget, dsb) tidak dilindungi sama sekali. Kalau satu device di company manapun memicu panic (misal nil pointer dari event WA yang tidak terduga), **SELURUH proses Go mati** — koneksi WA SEMUA company terputus bersamaan, bukan cuma company yang device-nya bermasalah. Diperparah karena proses production belum systemd (lihat Known Issues) — tidak auto-restart.

Rekomendasi: tambah `defer func(){ if r := recover(); r != nil { log... } }()` di setiap goroutine manual + di dalam `eventHandler` (per-event, bukan per-client), supaya 1 event/device yang error tidak menjatuhkan koneksi device lain. Tetap log panic-nya dengan jelas — `recover()` mencegah proses mati, tapi bukan pengganti perbaikan root cause bug-nya.

### 9.2 `g_backend`: tidak ada graceful shutdown (Risiko Sedang-Tinggi)

Tidak ada signal handling (SIGTERM/SIGINT) di `cmd/server/main.go`. Tiap restart/deploy, proses di-`kill` paksa — semua koneksi WA aktif terputus kasar (bukan logout bersih), termasuk risiko mid-write ke SQLite whatsmeow store. Sebaiknya dikerjakan bareng dengan setup systemd (checklist poin 13).

### 9.3 `WaApiKey`: `secret_key`/`token` disimpan plaintext (Risiko Tinggi)

`app/Models/WaApiKey.php` menyimpan kredensial API pihak ketiga polos di database, tidak di-hash. Kebocoran DB/backup langsung expose semua kredensial API semua company sekaligus. Rekomendasi: hash saat disimpan (pola sama seperti password), tampilkan nilai asli hanya sekali saat digenerate.

### 9.4 `POST /wa-api/v1/send-message`: tidak ada rate limiting sama sekali (Risiko Tinggi)

Dikonfirmasi bukan cuma belum lewat `BroadcastThrottleService` (sudah diketahui, lihat 7.1) — juga TIDAK ADA `throttle:` middleware Laravel maupun limiter apapun di sisi Go untuk endpoint ini. Otomatis ikut tertutup begitu sentralisasi `InboxService` (checklist poin 3) selesai — dicatat di sini supaya masuk scope test staging-nya.

### 9.5 Job/endpoint lain yang bypass proteksi kirim (Risiko Tinggi/Sedang)

Selain `WaApiSendMessageController` (7.1), ditemukan 2 jalur lain yang juga langsung panggil `InboxService` tanpa `requireActivePackage()`/`reserve()`/`BroadcastThrottleService`:
- **`app/Jobs/SendChatbotFlowMessages.php`** (Tinggi) — volume berpotensi tinggi (tiap step flow terpicu), risiko ban WA + kuota tembus tanpa batas untuk company yang package-nya sudah expired.
- **`app/Jobs/SendCsatSurvey.php`** (Sedang) — volume rendah (1x per percakapan resolved), risiko lebih kecil tapi pelanggaran pola yang sama.
- **`app/Http/Controllers/Api/GoogleFormWebhookController.php`** (Tinggi) — sama, DITAMBAH **tidak idempotent** (tidak ada dedup guard sama sekali): submit form yang terkirim dobel bisa bikin pesan WA dobel ke customer asli. Idempotency ini TIDAK otomatis ketutup oleh sentralisasi `InboxService` — perlu ditambah terpisah (checklist poin 4), ikuti pola `Cache::add` per submission id seperti `WaIncomingMessageWebhookController`.

Ketiganya (proteksi kuota/throttle-nya) otomatis terlindungi begitu checklist poin 3 selesai.

### 9.6 Index database untuk tabel log besar (Risiko Tinggi)

Tidak ada index `created_at` di 5 tabel log yang tumbuh terus lintas semua company dan selalu di-query `->latest()->paginate()`: `audit_logs`, `ledger_entries`, `history_user_login`, `voucher_histories`, `payment_transactions`. Kalau sudah jutaan baris, tiap buka halaman list-nya bakal full-table-scan + filesort. Sebagai perbandingan, `wa_conversations`/`wa_message_schedule_logs` sudah punya index yang tepat — tinggal terapkan pola yang sama ke 5 tabel ini (migration tambah index, risiko rendah untuk dikerjakan).

### 9.7 Temuan tambahan (Risiko Sedang/Rendah, dicatat untuk referensi — belum masuk checklist prioritas)

- **`WaApiKeyController::generate()/data()`** tidak cek device yang diminta benar milik company yang login sebelum bikin/baca baris `WaApiKey`. Saat ini masih "selamat" karena ada lapis kedua (`assertOwnership` di Go), tapi bukan defense-in-depth yang sesungguhnya — kalau logic Go itu berubah, celah ini langsung jadi IDOR beneran. (Sedang)
- Kombinasi `X-API-KEY` shared-secret di `g_backend` + CORS untuk browser — berpotensi bocor ke client-side kalau frontend Next.js benar-benar manggil Go backend langsung tanpa lewat Laravel. Perlu diklarifikasi dulu arsitekturnya sebelum jadi masalah nyata. (Sedang)
- `WaIncomingMessageWebhookController` log `$request->all()` — isi pesan customer + nomor telepon ter-log mentah, kontras dengan disiplin jalur outbound yang sudah tidak pernah log isi pesan. (Sedang)
- `DealController::index()` (papan Kanban CRM) fetch semua deal tanpa pagination; `ContactController::list()` cuma hard-cap 500 baris tanpa pagination sungguhan (kontak lama diam-diam tidak pernah muncul di list). (Sedang)
- Tidak ada timeout/retry eksplisit dari Laravel saat manggil Go backend (`InboxService`/`ConnectDeviceService`/`GolangAuthService`) — bergantung kontrak tak tertulis `sendSlotMaxWait=25s` di sisi Go, rapuh kalau salah satu sisi berubah. (Sedang)
- Dokumentasi `docs/api/wa-api-v1.openapi.json` untuk response 422 tidak cocok dengan response asli Laravel (`{"message":..., "errors":...}` vs yang didokumentasikan). (Sedang)
- JWT di `g_backend` (`auth-service.go`) tidak membatasi algoritma secara eksplisit (`jwt.WithValidMethods`) — defense-in-depth murah untuk dikerjakan. (Rendah)
- Nomor telepon/JID ter-log polos di beberapa tempat di `g_backend` (isi pesan sendiri sudah aman, tidak pernah di-log). (Rendah)
- Header `Content-Disposition` di `wa-media-controller.go` dibangun tanpa escape nama file. (Rendah)
- `trigger_count` di 3 tempat (`SendAutoReplyMessage`, `SendAiBotReply`, `GoogleFormWebhookController`) pakai baca-tulis non-atomik alih-alih `->increment()` — murni counter statistik tampilan, dampak kosmetik saja, bukan kuota/uang. (Rendah)
- `GoogleFormWebhookController` terima payload tanpa batas ukuran/jumlah field — relevan karena pola integrasi ini jadi TEMPLATE untuk integrasi pihak ketiga berikutnya (Visi Produk poin 8). (Rendah-Sedang)

### 9.8 Catatan: kenapa temuan terus berulang tiap audit

Dibahas 27 Agustus 2026 — dicatat supaya jadi konteks di sesi berikutnya, bukan sekadar keluhan. Pola "tiap audit selalu nemu yang baru" di 3 audit sejauh ini (14, 21, 27 Agustus) bukan soal audit kurang teliti — dua sebab konkretnya:

1. Cakupan tiap audit belum pernah sama persis. `g_backend` (Go) misalnya baru diperiksa pertama kali di audit 27 Agustus ini — bukan kelewat, memang belum pernah disentuh sebelumnya.
2. Proteksi kirim WA (`requireActivePackage`/`reserve`/`BroadcastThrottleService`) saat ini DITEMPEL manual di tiap pemanggil, bukan dipaksa lewat satu titik. Jadi tiap kali ada job/controller baru yang kirim WA (`SendChatbotFlowMessages`, `SendCsatSurvey`, `GoogleFormWebhookController`), developer harus INGAT copy proteksi itu ke file baru — kalau lupa atau fiturnya dibuat buru-buru, bolong lagi. Ini akar kenapa checklist poin 3 (sentralisasi ke `InboxService`) itu prioritas struktural, bukan cuma 1 dari 14 tugas: begitu semua jalur kirim WA WAJIB lewat 1 fungsi, kelas bug "jalur baru yang bypass proteksi" ini seharusnya berhenti muncul di audit-audit berikutnya, bukan lagi ditutup satu-satu tiap ketemu.

**Rekomendasi ke depan**: jalankan section 0 (Prinsip Dasar, 5 pertanyaan) SAAT menulis fitur/job baru yang menyentuh pengiriman WA — bukan menunggu audit berikutnya nemuin belakangan.


## 10. Known Issues (per audit 14 Agustus 2026, diverifikasi ulang 21 Agustus 2026)

Catatan untuk sesi mendatang — ini ditemukan lewat investigasi.

### Masih terbuka

1. **Dua sistem assignment chat berjalan sendiri-sendiri**: `WaContact.assigned_to` (lama, dipakai UI Inbox) vs `WaConversation.assigned_to` + status Chat Ops (baru, backend lengkap tapi belum ada UI). Perlu diputuskan mau disatukan ke arah mana.
2. Konsep "Category" untuk chat belum ada (baru ada Label). Pelacakan "assigned oleh siapa" (audit) juga belum ada di kedua sistem assignment.
3. Verifikasi `CACHE_STORE`/`QUEUE_CONNECTION` di `.env` produksi — pastikan bukan `file`/`array`/`sync` supaya rate limiter dan queue tetap aman untuk banyak worker. (Di `.env` lokal per 21 Agustus 2026 sudah `database` untuk keduanya — tetap cek ulang khusus di server production.)

### Sudah diperbaiki

- **Race condition di `DispatchDueWaMessageSchedules::claimAndDispatch()`** — sekarang sudah pakai `lockForUpdate()` + try/catch `QueryException`, pola yang sama dengan `PackageLimitService::lockOrCreateUsage()`. Diverifikasi 21 Agustus 2026.
- **`SendAutoReplyMessage` dan `SendAiBotReply` tidak melewati `BroadcastThrottleService`** — sekarang keduanya sudah memanggil `$throttle->attempt($deviceId, $companyId)` sebelum kirim, sama seperti `SendScheduledWaMessage`. Diverifikasi 21 Agustus 2026.
