<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('jobs', function (Blueprint $table) {
            // Auto-increment, NOT uuid: Illuminate\Queue\DatabaseQueue
            // never sets an `id` value itself when it inserts a job — it
            // relies entirely on the column auto-incrementing. A uuid
            // primary key with no default here makes every single
            // dispatch to the database queue fail with MySQL error 1364
            // ("Field 'id' doesn't have a default value"), which is what
            // silently broke Pesan Terjadwal / Balasan Otomatis / Auto
            // Reply (see 2026_08_03_080000_fix_jobs_and_failed_jobs_id_column
            // for the migration that corrects this on databases that
            // already ran this file with the old uuid column).
            $table->id();
            $table->string('queue')->index();
            $table->longText('payload');
            $table->unsignedTinyInteger('attempts');
            $table->unsignedInteger('reserved_at')->nullable();
            $table->unsignedInteger('available_at');
            $table->unsignedInteger('created_at');
        });

        Schema::create('job_batches', function (Blueprint $table) {
            $table->string('id')->primary();
            $table->string('name');
            $table->integer('total_jobs');
            $table->integer('pending_jobs');
            $table->integer('failed_jobs');
            $table->longText('failed_job_ids');
            $table->mediumText('options')->nullable();
            $table->integer('cancelled_at')->nullable();
            $table->integer('created_at');
            $table->integer('finished_at')->nullable();
        });

        Schema::create('failed_jobs', function (Blueprint $table) {
            // Same reasoning as jobs.id above — Laravel's failed-job
            // recording code doesn't generate this id either.
            $table->id();
            $table->string('uuid')->unique();
            $table->text('connection');
            $table->text('queue');
            $table->longText('payload');
            $table->longText('exception');
            $table->timestamp('failed_at')->useCurrent();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('jobs');
        Schema::dropIfExists('job_batches');
        Schema::dropIfExists('failed_jobs');
    }
};
